# Architecture Changelog

Architecture-level changes only — one line each. Routine copy/content edits do
**not** belong here. Dates are the merge/commit date.

**This file is history, not current state.** An entry was true on its date and is not updated
afterwards. For what is current V2, retired V1, transitional QR, or external, read
[`system-classification.md`](system-classification.md).

## 2026-08-15 (QR acknowledgement: the correction and removal promise deleted)

- **fix(gas-v2): the QR acknowledgement no longer promises that a reply will correct or remove a record, and the two Script Properties that gated that promise are gone.** `AXP_REPLY_TO_MONITORED` and `AXP_REMOVAL_PROCEDURE_CONFIGURED` existed only to keep those two sentences unrendered until a monitored mailbox and a written procedure existed. AxisPoint is not offering correction or removal as a feature, so the copy was deleted outright and the gates with it: `canPromiseReply`, `replyPromiseHtml`, `replyPromiseText`, `footerLegalFor`, both `PROP_KEYS` entries, both config fields, `isTrue`, and both `configHealth` blockers. The rendered email is byte-identical to what the flags-false configuration already produced, apart from the preheader, which said "Reply if anything needs correcting" unconditionally and now says only that the details were received.
- **`configHealth` keeps `blockers` and `promisesKeepable` with an empty list.** Nothing outbound now makes a promise configuration has to make true. The shape is kept, and reported by `doGet`, so the next piece of copy that promises something has somewhere honest to say so.
- **docs: both hard QR launch blockers in [`STATUS.md`](STATUS.md) are resolved by removal, not by provisioning.** No monitored Reply-To mailbox was set up and no removal procedure was written. `deployment.md`, `staging-provisioning.md` (now 13 staging properties, not 15), and `backend-v2-contract.md` §13 no longer list either property as something to set.
- **Scope: nothing else in QR changed.** The form, the Contacts tab, storage, the daily digest, and the matching logic are untouched.

## 2026-08-15 (V1 retirement, the deletion pass)

- **V1 is fully retired. There is no V1 backend, no V1 form, and no V1 email system left in this repository.** This is the deletion pass that PR #79 was written to make safe, executed from base commit `71d345d`. 55 tracked files removed, 15,988 lines. The full inventory, with counts, is the retirement record in [`system-classification.md`](system-classification.md); this entry is the summary. **These two numbers were originally recorded as 78 files and about 17,000 lines, which was wrong in a specific and repeatable way:** they were read off the pass's overall diffstat (84 files changed, 16,990 deletions), which counts every file the pass touched and every line it removed from files that still exist. The correct figures count only files actually deleted, and are reproducible with `git diff --diff-filter=D --numstat 71d345d 68bc44a`.
- **chore(gas): the V1 Apps Script backend deleted, all 29 tracked files under `scripts/gas`.** `Code.gs`, `appsscript.json`, `.claspignore`, the eight email template mirrors, and the 18 files under `tests/` (15 suites, two helpers, a README). The root `test:gas` and `gas:push` scripts went with them. **`.github/workflows/test-gas.yml` was kept, not deleted:** it ran two suites, and the V2 one still needs it. Only the V1 step was removed, after verifying the workflow had a second purpose.
- **chore(brand): the V1 form tree deleted, all 19 tracked files under `packages/brand/src/components/form`,** plus `utils/vcard.ts`, `team.ts`, and `types.ts`. The barrel `index.ts` is now `Mark` and `E2eBanner`, which is what `apps/web` and `apps/qr` actually import, verified against every `@axispoint/brand` import in the workspace rather than against the prior audit. The `./team` and `./types` subpath exports were dropped from `packages/brand/package.json`; `./colors`, `./fonts`, and `./tailwind-preset` stay, because the Tailwind preset is consumed by both apps' `tailwind.config.js`. `packages/brand/.eslintignore`, which existed only to skip the form tree, was deleted as its own comment instructed.
- **chore(web): `SharePage.tsx` and the `/share/:code` route deleted outright, with no redirect.** A redirect would have preserved V1 behaviour under a new name, and the route was never a published address of this build. **`refToken` is untouched** on the wire, in the intake, and in the backend: its producer is gone, but `Intake.tsx` reads `?ref=` straight off the query string, so nothing about attribution changed.
- **chore(web): the abandoned articles residue deleted.** `content/articles`, `content/publications`, and the `gray-matter`, `remark`, and `remark-html` dependencies, removed from `apps/web/package.json` and from `pnpm-lock.yaml` (75 packages dropped). Recorded separately from V1 because it was never part of the V1 backend or form.
- **docs: `PARTNER_CONTACTS.md` created, and it is the reason `team.ts` could be deleted rather than merely emptied.** The owner confirmed the current partner email addresses and phone numbers directly. The classification document had ruled that `team.ts`'s literals were historical, not verified, and must not be copied into current code or a tracked document without owner verification. That verification happened, so the confirmed values are tracked and the unverified ones are gone.
- **docs: the four V1-only operating documents moved to `docs/archive/` with an ARCHIVED banner each.** `backend-architecture.md`, `email-templates.md`, `frontend-payload-schemas.md`, and `UNIFIED_SCHEMA_MIGRATION_PLAN.md` described source that no longer exists; leaving them in `docs/` would have left four current-looking documents describing a deleted system. `docs/deployment.md` lost its V1 operating sections, **including the V1 Script ID, Deployment ID, `/exec` URL, and bound Spreadsheet ID**, which now live only at the tags, exactly as `archive/deployment-v1.md` intended. Its calendar-access gotcha was kept and rewritten against V2, because `scripts/gas-v2/src/GoogleServices.js` inherits the same `executeAs` mechanism.
- **Historical state: `v1-stable` for V1's final source, `pre-v1-retirement-2026-08-14` for the complete pre-deletion repository.** Both on `origin`. Reading a V1 identifier is now a deliberate act against a tag rather than a lookup in the tree.
- **Out of scope by instruction, and left alone:** `apps/qr` product code and the whole QR retain/retire/rebuild decision, `scripts/gas-v2`, and the `VITE_FORM_ENDPOINT` rejection guards in both `vite.endpoint.ts` files, which are negative safety guards against obsolete configuration rather than V1 runtime code.
- **Not touched, and not touchable from here:** the external V1 Apps Script project, its Sheet, and its deployment. Deleting the source does not disable them. That is a separate owner decision and a separate authorized operation.

## 2026-08-15 (No-deletion verification and documentation safety pass)

- **docs: `system-classification.md` is now the single source of truth for what is V1, V2, transitional QR, or external.** `CLAUDE.md`, `AGENTS.md`, `README.md`, `STATUS.md`, `deployment.md`, and this file point at it instead of each carrying its own copy. Five documents held five versions of that boundary and had drifted apart; the classification's own update rule is that any PR moving a system between categories updates it in the same PR.
- **docs: a V1 deployment preservation record exists before any V1-only instruction is edited.** `docs/archive/deployment-v1.md` summarises the V1 project shape in prose and names `v1-stable` and `pre-v1-retirement-2026-08-14` as where the full state lives. It deliberately reproduces no Script ID, Deployment ID, Spreadsheet ID, `/exec` URL, or `.clasp.json` content: those stay in the tags and in the still-unedited `deployment.md`.
- **docs: the stale-documentation cluster corrected against current source.** `apps/qr` has read `VITE_V2_SUBMISSION_ENDPOINT` since PR #70 and does not import the V1 `ContactForm`; `scripts/gas` is retired and not serving current business traffic; and the V2 rebuild is implemented through PR #78 rather than still pending. Each claim was re-derived from the file that settles it, not from the previous document.
- **fix(qr): the e2e warning banner is mounted again.** `vite.config.ts` computed `__E2E_MODE__` and printed the terminal warning, but `main.tsx` had stopped rendering `<E2eBanner />` when the V1 form was removed, on reasoning that stopped being true once the Contact Exchange submitted to a real backend. Half the warning was live and half was silently absent, and nothing failed because no test had ever rendered this app's tree. A new `Root.tsx` holds the tree so a test can render it. **Additive to the existing fail-closed guards, not a replacement:** a missing endpoint or a lone V1-style value still throws at config time.
- **feat(infra): CI now runs the gates that already existed.** `pnpm test:frontend` (246 tests), the seven-route rendered English baseline, the rendered ARIA assertions, the 20-state intake baseline, and production-bundle inspection of **both** `apps/web/dist` and `apps/qr/dist`. All of these were committed, green locally, and unrun by any workflow for several passes. Three jobs so a failure names what broke.
- **feat(infra): lint covers all four workspace packages.** `apps/qr`, `packages/brand`, and `packages/submission-client` had no `lint` script, so `pnpm -r lint` was linting one package in four while reading as a workspace gate. The retired V1 form tree is excluded by `packages/brand/.eslintignore` rather than edited, because this pass deletes nothing.
- **fix(infra): the intake-state harness runs on CI.** It hardcoded a macOS Chrome path and assumed an already-running dev server on port 3000, which is why its 20 committed baselines had never been checked by anything but a hand run. It now resolves Chrome or Chromium on macOS and Linux, honours `CHROME_PATH`, starts and stops its own Vite dev server, and takes free ports for both the server and the DevTools endpoint. The frozen clock and the profile-scoped cleanup are unchanged.
- **fix(infra): `ci.yml` passed the V1 endpoint variable into both builds.** It set `VITE_FORM_ENDPOINT: ''`, which neither app reads, so the intended "build with no endpoint" was true by accident rather than by instruction. The two **deploy** workflows have the same defect and were deliberately left alone as deployment configuration; it is recorded in `deployment.md` and `STATUS.md` as required work for whoever adds the FTP secrets.
- **The e2e banner's copy is now forbidden in a production bundle** by `inspect-bundle.mjs`, and asserted present when the flag is on by `apps/qr/tests/e2eBanner.test.ts`. Verified in both directions, including a build with `__E2E_MODE__` forced true to confirm the gate has something to catch.

## 2026-08-13 (Multilingual Content Rollout, PR 5 of 5)

- **feat(web): the URL is the only source of truth for page locale.** English unprefixed, every other locale path-prefixed, `/en/...` refused so English keeps one canonical address. Nothing is persisted to a cookie or `localStorage`, because a stored locale can disagree with the URL and then one of them loses silently.
- **feat(web): only `enabled && reviewed` locales produce routes, canonicals, `hreflang`, or sitemap entries**, which today is English alone. A disabled prefix renders the 404 inside the normal chrome rather than falling back to English, so the launch gate stays visible and no unavailable URL gets published.
- **fix(web): two routing defects caught by the committed rendered-English baseline.** React Router ranks a dynamic segment above a splat, so `/no-such-page` matched `/:locale` and rendered the home page; and the refused-locale 404 first rendered with no header, footer, or skip link.
- **fix(web): script fonts now reach page content, not just `body`.** Headings carry `font-serif` (Cormorant Garamond), which sets its own family and outranks inheritance, and Cormorant has no Devanagari, Gujarati, Gurmukhi, Arabic, or CJK glyphs, so those headings were drawn by an arbitrary substitute. A `[lang]`-scoped stylesheet now covers headings too, emitted only for the six non-Latin locales so Spanish and Vietnamese keep the brand serif.
- **feat(gas-v2): audit-candidate visitor email translations, structurally unable to deploy.** The acknowledgement and booking confirmation have model-generated sets for the eight unreviewed locales in `scripts/gas-v2/audit/`, outside the `.claspignore` allowlist (`appsscript.json` + `src/*.js`), so they cannot reach the Apps Script project. `resolveOutboundLocale` still only returns a locale in `LAUNCH_READY_LOCALES` (`['en']`): a visitor asking for Urdu is recorded as wanting Urdu and answered in English.
- **verify(gas-v2): the booking instant is byte-identical in all nine locales.** The translations are applied over the one reviewed `formatInstant` output, so a localized email cannot name a different hour. Substitution is checked in both directions: a phrase that no longer exists throws, and a phrase that survives substitution throws.
- **fix(gas-v2): plain-text emails are not the HTML with tags stripped.** `textRule` uppercases headings and `textWrap` inserts newlines mid-phrase, so the first substitution silently did nothing to the text body and would have sent English headings and paragraphs beside translated labels. Matching is now case-aware, whitespace-flexible, and word-boundary anchored.
- **docs: path-prefixed URLs are unproven on the real host.** This repository contains no rewrite configuration of any kind and the frontend deploys by FTP to Apache `public_html/`; all routing evidence comes from the dev server's SPA fallback. Verifying or configuring the host rewrite is a prerequisite for activating any non-English locale. No rewrite file was invented.

## 2026-08-11 (Multilingual Content Rollout, PR 4 of 5)

- **feat(web): the contact page, intake, validation, submission states, and booking are catalog-driven in all nine languages.** 108 new keys, catalog now 366, all eight audit catalogs at full parity. Rendered English is identical across a 20-state baseline and all seven routes.
- **feat(infra): a deterministic 20-state intake baseline, captured before any edit.** `intake-states.mjs` drives a real browser because step 2, `blocked`, and the booking selection cannot be reached by URL, and it **freezes the clock** at a fixed instant, without which booking candidates would make the committed baseline rot within a day. Two harness defects were caught by spot-checking rather than trusted: a `:nth-of-type` selector that clicked the date group twice and left the booking unselected while looking plausible, and the un-frozen clock.
- **feat(web): one interpolation helper with seven declared placeholders.** `interpolate` substitutes `{name} {email} {count} {day} {time} {mode} {language}` and leaves an unknown token visible rather than blanking it, so a bad translation fails loudly instead of rendering a gap. Tests assert placeholder declaration, cross-locale placeholder parity, and that no locale resolves to a leftover brace.
- **verify(web): the booking instant is locale-independent while its label is not.** All nine locales select the same 11:00 slot and render it as `11:00 AM`, `11:00`, `上午11:00`, or `11:00 am`. An earlier version of the check compared labels directly and failed; that was the check being wrong, since localising the label is the requirement.
- **feat(web): `Select a date` and `Select a time` are translated `aria-label`s**, so the browser review locates the pickers structurally rather than by English text, which would have silently no-opped in eight of nine locales.
- **verify(web): 189-observation nine-locale intake review** at two widths. Zero console errors, zero page exceptions, zero non-font off-origin requests, no English fallback, no unresolved placeholders, Urdu the only RTL locale, and no overflow beyond the documented 3px chrome case. Per-locale review artifacts committed at `apps/web/tests/preview-intake/`.

## 2026-08-11 (Multilingual Content Rollout, PR 3 of 5)

- **feat(web): the five marketing pages are catalog-driven in all nine languages.** Home, Property Management, Asset Management, Investor Services, and Partners, plus the page titles and meta descriptions that originate in those components. 143 new keys, catalog now 258, all eight audit catalogs at full parity. Rendered English is byte-identical to the committed baseline.
- **feat(web): shared keys rather than duplicated strings.** Service names and the management-proposal call to action reuse the existing `nav*` keys; `pmRunsAmDirectsTitle` is one key shared by the home page and Asset Management, `partnersSignature` by the home page and Property Management. Two keys holding the same English is how surfaces stop matching at the first translation.
- **fix(web): partner names are never transliterated.** Hindi initially rendered them in Devanagari while `PartnersPage.tsx` renders them in Latin for every locale, which would have shown a reader two spellings of the same person on one page. Names, `AxisPoint`, the email address, and the numeral `404` stay in code rather than in the catalog.
- **feat(infra): per-locale review artifacts now cover every page.** `apps/web/tests/preview/<code>.txt` holds the chrome once and then all five marketing pages, 237 lines per locale, so a native reader opens one file and reads their whole language in reading order.
- **verify(web): 90-observation browser review** across five pages, nine locales, and two widths in an isolated headless Chrome over CDP. Urdu is the only `dir="rtl"` and mirrors correctly on every page at both widths; `<html lang>` correct in all 90; **no page-specific overflow introduced** (`<main>` overflow 0px everywhere, page-level overflow only the documented pre-existing 3px chrome case, identical in English); every locale renders a translated `<h1>` so no page fell back to English; zero page exceptions; non-font off-origin requests 0.
- **audit: React warns that it does not recognise the `fetchPriority` prop**, 18 times, from `PageParts.tsx:97` and `HomePage.tsx:82`. React 18 does not support the attribute. Pre-existing and untouched by this PR; PR 2's review never saw it because the 404 route has no images. Recorded, not fixed.
- **note: no structured metadata exists in the application**, so none was migrated and none was invented. `ContactPage.tsx` is the intake's shell rather than a marketing page and moves with the intake in PR 4.

## 2026-08-10 (Multilingual Content Rollout, PR 2 of 5)

- **feat(web): the site chrome is catalog-driven in all nine languages.** Header, footer, shell, 404, and the language selector's assistive labels: 23 keys, total now 115. The header and footer share keys for the names that appear in both, because two keys holding identical English is how they drift apart at the first translation. Rendered English is byte-identical to the committed baseline.
- **feat(web): assistive labels are translated, not left English.** `aria-label` on the lockup, primary navigation, menu dialog, close button, and both language-selector controls. A screen-reader user hears these in place of seeing the layout, so an untranslated one is a worse failure than an untranslated visible string, not a lesser one.
- **feat(web): one interpolated catalog string**, `languageChooseAria`, with a literal `{language}` placeholder and a `String.replace` helper. Deliberately not a formatting library or ICU syntax; one placeholder justifies neither, and an unfamiliar message syntax is one more thing a translator can get wrong.
- **feat(infra): committed rendered locale previews** at `apps/web/tests/preview/<code>.txt`, one per locale, produced by `render-locale-preview.mjs`. They render the 404 route, the smallest page containing every chrome surface, with direction, font stack, and review status stated. Native readers should review these rather than the TypeScript catalogs. Expected to change as translations are corrected, unlike the English baseline.
- **feat(infra): a no-orphan-key test.** Every catalog key must appear in application source, so a key nothing renders fails instead of quietly becoming copy a translator maintains for nobody.
- **docs: the baseline's scope is now stated.** `verify:baseline` compares rendered text with attributes stripped, so it proves visible copy did not move and does **not** cover `aria-label` values.
- **verify(web): nine-locale browser review at 390px and 1512px**, in an isolated headless Chrome driven over the DevTools Protocol from Node's built-in `WebSocket`. No dependency added; unique temp profile and debugging port, cleanup limited to the spawned PID and that profile. Urdu `dir="rtl"` and a correctly mirrored header confirmed at both widths, `<html lang>` correct in all nine, no footer overflow, zero console errors, zero page exceptions, and no off-origin traffic beyond Google Fonts. The three menu-open labels (`navMenuDialogAria`, `navCloseMenu`, `languageListAria`) are confirmed translated in all nine locales by actually opening the menus.
- **audit: the per-locale `fontStack` is never applied to page content.** Its only consumers are three sites inside `LanguageSelector`; `LocaleProvider` sets `lang` and `dir` alone. Computed `font-family` on navigation is `Figtree, sans-serif` in every locale, so translated chrome renders through an arbitrary system fallback rather than the approved Noto family. Not caused by PR 2, which made it visible rather than introducing it. Recorded for PR 5, which owns fonts.
- **audit: a pre-existing 3px horizontal overflow at 390px.** The header's right-hand control cluster ends at x=393 in a 390px viewport. A dedicated probe shows it constant and locale-independent, present with English and no preview gate, and unrelated to the cycling decorative word. Not caused by PR 2. Recorded as a known risk and left for a separate fix.
- **feat(infra): `pnpm verify:aria` asserts the rendered assistive labels.** It renders the real component tree in five locales and reads the actual `aria-label` attributes, including that `{language}` is substituted rather than announced aloud. Type-checking and the orphan test do not cover this: a negative control that rebinds one label to a different valid key passes both while the screen reader says the wrong thing. It also **prints the three labels it cannot reach** (`navMenuDialogAria`, `navCloseMenu`, `languageListAria`), which sit behind `{open && ...}` branches that a static render never enters, rather than reporting a pass over half the surface.

## 2026-08-10 (Multilingual Content Rollout, PR 1 of 5)

- **feat(web): eight audit-candidate locale catalogs, all 92 keys, none reviewed.** Spanish, Simplified and Traditional Chinese, Vietnamese, Hindi, Urdu, Gujarati, and Punjabi now exist under `apps/web/src/i18n/catalogs/audit/`. **The text is model-generated and no speaker of any of these languages has read it.** Not approved, not professionally translated, not native-reviewed, not production-ready. All eight remain `enabled: false` and `review: 'unreviewed'`, so nothing is advertised, routed to, or indexed. They exist to give a native reader something to correct.
- **feat(web): audit catalogs are excluded from every build artifact structurally, not by tree-shaking.** `vite.config.ts` aliases the audit loader to a stub whenever Vite's `command` is `build`, so the directory leaves the module graph in **every** mode rather than only `production`. Gating on mode would fail open for any mode invented later. The accepted consequence is that no built preview of an unreviewed locale exists; preview runs on the dev server.
- **fix(infra): the production-bundle gate was reading a fraction of the output.** `inspect-bundle.mjs` inspected `dist/assets/*.js` only, while `sourcemap: true` is set. A module tree-shaken out of the JavaScript still carries its full original source in the adjacent `.map`, so the gate could pass while the content it looked for shipped one file over. It now walks every emitted file recursively and additionally parses sourcemap `sources` arrays. Two independent negative controls prove it fails on a planted content leak and on a planted sourcemap-path leak.
- **feat(web): per-key catalog fallback replaces all-or-nothing.** A catalog is a `Partial<Messages>` and each key falls back to English independently, so a half-reviewed locale renders reviewed copy where it exists and correct English elsewhere instead of blanks. Empty string counts as missing, which is what a skipped translation row exports as.
- **feat(web): a documented promotion path.** `catalogs/reviewed/` is statically imported and always in the production graph; `catalogs/audit/` never is. Promoting a locale is moving one file, adding one line, and flipping two registry flags. No build-configuration change is involved, and reviewed content always beats an audit candidate for the same locale.
- **feat(infra): a committed rendered-English baseline.** `apps/web/tests/baseline/` holds the rendered text of all seven routes and `pnpm verify:baseline` enforces it, which is what lets PRs 2 to 4 migrate ~320 strings without repeating the bulk-substitution failure that was reverted last pass. The harness renders through Vite SSR because Node's type-stripping does not transform JSX, so the existing runner cannot render a `.tsx` component (verified on Node v25.3.0).
- **docs: the required-dependencies table said seven Noto families; it is six.** Spanish and Vietnamese use Figtree and need no script family. `PREVIEW_FONT_HREF` has always requested six.

## 2026-08-09 (Localization Readiness)

- **fix(web): both Chinese follow-up preferences were being silently discarded.** The intake select offered "Chinese (Simplified)" while the mapper matched on "Simplified Chinese". Neither matched, the lookup fell through to `null`, and the preference vanished before reaching the wire. Nothing failed and nothing logged. The select now stores locale codes, and a regression test drives the actual option values, so a mismatch between what is shown and what is mapped fails in tests instead of in production.
- **refactor(web): one canonical locale registry.** `apps/web/src/i18n/locales.ts` is now the only list. The intake's duplicate `APPROVED_LANGUAGES` and the mapper's third English-name table are deleted. Three copies of one list is what let two spellings drift.
- **fix(web): the selector's choice now reaches submissions.** Locale state moved from navigation-local to app level, so the intake stops hardcoding `pageLocale: 'en'`. Page locale and follow-up preference are carried and stored as two separate facts.
- **fix(web): six intake controls were storing English labels as their wire key.** Property type, scope, situation, involvement, timing, and both topic pickers held their **display label** in draft state, and the mapper looked the token up by that label. Translating the UI would have broken every Management Proposal submission and silently mis-stored involvement and scope. Each control now carries a stable snake_case token plus a catalog key; draft state, conditional logic, summaries, and the envelope use the token, and the mapper's six label-keyed tables are deleted. A synthetic catalog changes every visible label in a test and the submitted envelope is unchanged.
- **feat(web): a typed message catalog, English only.** Deterministic fallback to English by one rule, plus completeness checks that fail in development and tests rather than rendering blanks. 92 keys, every one consumed: the control label sets, the gateway, the two short pathways, validation, summaries, and booking chrome.
- **The wider copy migration was attempted and reverted, deliberately.** A bulk JSX substitution would have wired step headings, field labels, placeholders, and the submission-state screens to the catalog. It was reverted on review: a bulk replace leaves a partial rewrite wherever a mapping misses, and one of its value corrections would have changed a visible short-pathway label (`organizationLabel`) as a side effect. The 49 unused keys it had added were removed with it. Those screens remain hardcoded English, and the **Multilingual Content Rollout** pass owns migrating them screen by screen against rendered output.
- **feat(web): booking display follows the reader, the instant does not.** Day and time labels take the active locale. `slotStart` is still computed in the firm's zone with a real offset, so changing display language cannot move a meeting. The two `Intl` calls used for offset arithmetic are annotated so nobody localizes them later.
- **fix(gas-v2): outbound locale resolution is actually connected.** `resolveOutboundLocale` and `LAUNCH_READY_LOCALES` existed since Pass 9A with no caller: a Spanish preference was stored, shown to a partner, then had no effect on the template. Acknowledgement and booking-confirmation handlers now dispatch through it. English stays the only real template set, and internal partner mail takes no locale parameter at all.
- **Nothing was translated and no non-English locale was enabled.** No machine translation, no fabricated Spanish copy. A synthetic catalog and template set exist for tests only, and `verify:bundle` now asserts neither reaches a production bundle.
- **Verification: 102 client, 76 web, 27 QR, 445 gas-v2, 204 V1.** Browser-verified at 320/390/430/834/1512 with unchanged English behaviour, plus a seam check proving `<html lang>` and `dir` flip to `ur` and `rtl`, both Chinese codes survive selection, and Urdu booking labels render while the instants stay identical. Twenty rendered English intake screens were captured before the refactor and re-captured after: **all twenty are identical**, which is what proves no copy moved.
- **Honest limits recorded in STATUS:** the intake catalog is partial and the remaining intake screens plus roughly 1,500 lines of marketing copy still need migration, no translated content exists, native-reader review is still required, and the locale routing decision remains open and uninvented.

## 2026-08-06 (Staging preflight: minimal OAuth scopes and the removal procedure)

- **fix(gas-v2): the manifest no longer requests full mailbox access.** `https://mail.google.com/` grants read, send, and **delete** across the deploying account's entire mailbox; `MailApp.sendEmail` needs only `https://www.googleapis.com/auth/script.send_mail`. Replaced before any consent screen was shown to anyone.
- **fix(gas-v2): three more requests removed, each verified unused first.** `script.scriptapp` (`ScriptApp`: 0 occurrences; triggers are created in the UI, which does not require the script to hold it), `script.external_request` (`UrlFetchApp`: 0 occurrences), and `calendar.events` (a strict subset of `calendar`). The manifest now requests exactly `spreadsheets`, `calendar`, and `script.send_mail`.
- **fix(gas-v2): the unused advanced Calendar service is removed.** The manifest enabled `Calendar` v3 while the code used only `CalendarApp`. An enabled advanced service must be turned on in the Cloud project and widens the consent screen, so carrying an unused one is a real cost.
- **test(gas-v2): the scope set is now pinned, not merely plausible.** The previous check only validated scopes it already knew about, so `https://mail.google.com/` fell through its loop unexamined and every suite passed. New tests assert the **exact** set, that no broad mail or Gmail scope appears, that every requested scope is backed by an API the code calls, that the removed ones stay removed **and** stay unused, and that no advanced service is enabled.
- **docs: both promise flags stay `false`, and no removal procedure is built.** `AXP_REMOVAL_PROCEDURE_CONFIGURED` and `AXP_REPLY_TO_MONITORED` are both false, so the QR acknowledgement omits its correction and removal lines and nothing is displayed or sent promising that information will be corrected or removed. One operational note is recorded instead: AxisPoint retains voluntarily submitted business information for normal operations, and any rare information request is handled manually by Zach on a case-by-case basis. **This is not an approved legal-compliance policy.** No automated removal system exists or is planned, and no deadline or commitment is set.
- **docs: the staging provisioning plan is recorded** in `staging-provisioning.md`: project and ownership, the six tabs with exact headers (Submissions is **48** columns, not 49; an earlier count included a quoted phrase from a block comment), **15** Script Properties, the dedicated staging calendar, triggers, deployment settings, safe recipients, the E2E matrix, and rollback. Live V1 identifiers are deliberately not repeated. The slot-taken case is documented as requiring a **deliberately seeded blocking calendar event** that must be deleted during cleanup, since the refusal cannot be produced by the form alone.
- **Status: repository-only.** No Apps Script project, Sheet, calendar, trigger, deployment, endpoint, email, or calendar event was created, contacted, or modified. This is the **Staging** pass named in STATUS, not a new numbered code pass.

## 2026-08-05 (Code Pass 10C: booking command connected)

- **feat(web): the booking command is connected through the shared submission client.** Choosing a time issues a real `booking_request` carrying the `leadId` the submission returned. It is a separate command after the submission, never a block inside one. **No backend change was required**: a read-only audit confirmed the request shape, response shape, refusal codes, and idempotency rule were already complete, and `scripts/gas-v2` is untouched.
- **Backend eligibility stays authoritative in both directions.** The call is offered only when `bookingEligible` came back true on the submission response, and `executeBookingCommand` re-evaluates `isBookablePathway` against the stored Lead when somebody actually books. No pathway policy exists in the frontend.
- **feat(web): candidate times are policy-valid requests, not claimed availability.** V2 exposes no availability query, so the browser cannot know what is free and no longer pretends to. The picker derives candidates from the backend's rules (business days, 09:00 to 17:00, 60-minute lead, 60-day horizon, 30-minute slots, `America/Chicago` with real daylight-saving offsets) and never draws a slot as taken. The hard-coded "August 2026" fixture, its invented taken-slots, and its grey-out-before-the-10th rule are deleted: all three were fabrications, and the fixed month would eventually have been refused with `SLOT_TOO_FAR_AHEAD`. New drift tests read every mirrored constant out of the real backend source, and run each generated `slotStart` through the backend's own `isWithinBusinessHours`.
- **A taken slot is an ordinary answer, not a failure.** `SLOT_UNAVAILABLE` renders the approved neutral line, "That time is no longer available. Please choose another.", and offers no retry, because resending the same request would be refused again.
- **Retry preserves `bookingRequestId`; a material edit replaces it.** An unchanged retry reuses the id so a timeout cannot create a second calendar hold. Changing the slot or the mode mints a new one, so the backend cannot replay the old booking and confirm a time the visitor moved away from.
- **fix(submission-client): the simulator was shipping in production bundles.** `createTransport` returned the simulator when `isDevelopment` was true, and that branch was removed in production only because the bundler inlined the function and folded the flag, which held while one call site existed. Adding the booking client ended the inlining and the simulator's fake lead ids and every fixture name appeared in the production bundle; `verify:bundle` caught it. **Fixed structurally**: `createTransport` can no longer return the simulator at all, and each app reaches it directly behind its own `import.meta.env.DEV` literal, so production never references it.
- **chore(infra): removed a bundle assertion that was not a safety guarantee.** The Pass 10A check "no endpoint configured, yet the network transport is still in the bundle" measured whether the bundler inlined `createTransport`, not whether the build was safe, and it began failing once a second client existed with nothing unsafe changed. The checks that do prove safety remain: no endpoint literal, the fail-closed `NOT_CONFIGURED` path present, no dev fixtures, and the `--expect-endpoint` direction.
- **Two mechanical QR compatibility changes.** `ClientSuccess` now covers booking responses, so `apps/qr/src/exchange/useExchange.ts` gained one narrowing guard, and `submissionClient.ts` adopted the same explicit `buildTransport` pattern as the fix above. Neither is a QR redesign; the Contact Exchange surface is unchanged.
- **Verification: 102 client, 47 web, 27 QR, 421 gas-v2 and 204 V1 tests, all passing.** Browser-verified at **320, 390, 430, 834, and 1512**: 40 intake renders and 25 booking renders, covering the candidate grid, a confirmed booking, a taken slot, a retryable calendar failure, and an unconfigured calendar, with zero console errors, zero off-site requests, zero storage writes, no sub-44px targets, and overflow measured against the untouched homepage baseline.
- **Status unchanged otherwise: nothing external happened.** No backend change, endpoint, deployment, real calendar, Google resource, or real E2E run. One shared `AXP_CALENDAR_ID` remains the calendar decision. Development stays simulated with zero network, and a production build without `VITE_V2_SUBMISSION_ENDPOINT` fails closed.

## 2026-08-05 (Code Pass 10B: QR Contact Exchange frontend)

- **feat(qr): the approved QR Contact Exchange is implemented and connected to the shared client.** `apps/qr/src/exchange` submits `submissionKind: 'contact_exchange'` through `packages/submission-client`. **Both apps now go through the one transport boundary**, and neither contains a second transport, a second wire-token list, or a `fetch` of its own. Built from `AxisPoint QR Contact Exchange.dc.html` (design@2026-08-01): the additive "Share your details" action under Save contact, one full screen rather than a sheet, three required answers and three optional, and the full validation, sending, success, failure, and retry state set.
- **The backend needed no change to accept it.** A read-only preflight against `scripts/gas-v2` confirmed the seven category tokens, the payload shape, the card slugs, and the phone and email rules already matched before any code was written. `scripts/gas` and `scripts/gas-v2` are untouched.
- **A QR submission creates a Submission and a Contact, never a Lead.** The category is a filing label, not a router: it creates no service lead, and changes no copy, route, or outcome. Proven where the rule is enforced rather than where it is avoided, by running a parsed exchange through the real `buildContact` and asserting the real `buildLead` throws.
- **Gathered-through attribution stays separate from ownership.** The browser sends only the scan (`sourceCategory: 'qr'` plus the card slug); the backend derives the immutable `acquisitionSource` and `scannedPartner` and leaves `ownerPartner` empty for every record, because a scan gives a partner a name, not a claim. An unrecognised slug resolves to `unknown` and is deliberately not rewritten to the firm, since it is evidence a printed card is wrong. A test confirms the backend rejects a browser-supplied owner as a server-owned field.
- **The category control is the approved native-select fallback**, taken under the board's own fallback clause. No component library is introduced, so the platform supplies the keyboard, type-ahead, and assistive-technology behaviour instead of code written for one page. The board's only objection to the native control, iOS truncating the longest label at 320px, is answered by echoing the full selected label as wrapping text beneath it.
- **feat(qr): `apps/qr` reads `VITE_V2_SUBMISSION_ENDPOINT`, not the V1 `VITE_FORM_ENDPOINT`.** The old name is the V1 deployment and speaks a different payload shape. Resolution moved to `apps/qr/vite.endpoint.ts` so it is unit-testable against fixture env directories rather than against a machine-local secret.
- **fix(qr): the error-summary jump links were 21px tall**, under the approved 44px floor. Found by browser measurement, not by reading the code.
- **Verification: 27 new QR tests, 78 client, 33 web, 421 gas-v2 and 204 V1 unchanged.** Every envelope is validated by the **real** `parseEnvelope` loaded into a VM, including all seven categories and the token list read from the backend rather than restated. Browser-verified at **320, 390, 430, 834, and 1512** (45 renders per run) against the dev simulator and a fail-closed production build, with zero console errors, zero off-site requests, zero storage or cookie writes, no data in URLs, and no sub-44px targets. The retry contract was verified on the wire against a local stub: two POSTs, same `submissionId`, byte-identical payload and attribution.
- **Status: nothing was added beyond the surface.** No automatic matching, merging, Google Contact sync, booking, storage persistence, or referral resolution. No endpoint, Apps Script project, Sheet, trigger, deployment, `.clasp.json`, or Google resource exists; no real E2E was run. Development is simulated with zero network, and a production build without the endpoint fails closed rather than simulating success.

## 2026-08-05 (Code Pass 10A: shared submission client and website-intake connection)

- **feat(submission-client): one package is now the only place a submission may be sent.** `packages/submission-client` owns the wire contract, attempt identity, error classification, and transport selection. Deliberately not in `packages/brand`: brand holds presentation, and a transport deciding retry semantics while holding PII in memory is not presentation. **Only `apps/web` consumes it**; the package supports `contact_exchange` and is shaped for the future QR connection, but `apps/qr` does not import it.
- **feat(web): the V2 endpoint has its own environment variable, `VITE_V2_SUBMISSION_ENDPOINT`.** `VITE_FORM_ENDPOINT` still names the **V1** deployment, which speaks a different payload shape; inheriting it would have aimed the V2 intake at the V1 backend and the failure would have read as a backend bug rather than a configuration mistake. In e2e mode a lone V1 value is a hard error that names the problem instead of a silent default. `apps/qr` still uses the V1 name and was not modified.
- **fix(brand): the e2e banner no longer claims a submission creates a calendar event.** It creates records and sends mail; booking is a separate command a submission does not invoke. Overstating the blast radius trains people to discount the warning.
- **fix(submission-client): a success response is validated before anyone is told it worked.** `ok: true` was accepted on the strength of a boolean `bookingEligible` alone. The client now requires the schema version, a matching `submissionId` and `submissionKind`, strict boolean `replay` and `bookingEligible`, correctly typed `leadId`/`contactId`/`slaDueAt`, and the kind-specific identity shape (an inquiry has a Lead and no Contact, an exchange has a Contact, no Lead, and no SLA). Failures become `MALFORMED_RESPONSE`, retryable, with the `submissionId` preserved so a retry is a replay. This catches a cached or generic JSON 200 from an intermediary, a future schema version, and a response for a different attempt.
- **fix(submission-client): a malformed error body no longer defaults to `INTERNAL_ERROR`.** That invented a backend verdict nobody gave, and a retryable one. An `ok: false` body must carry a non-empty string `code` and a `field` that is **present** and either a string or null, or it is `MALFORMED_RESPONSE`. An absent `field` is not silently read as null: `null` has to be written down, because "the key is missing" and "no particular field is at fault" are different claims.
- **feat(web): the V2 intake submits a real `schemaVersion` 1 envelope.** `apps/web/src/intake` maps its display labels to wire tokens through explicit tables that **throw** on an unmapped value rather than passing a label through, and derives `involvement` from `serviceScope` so the two can never contradict. Booking is never included in a submission.
- **The same-id retry requirement from the contract is implemented, not just documented.** `retry()` resends the same `submissionId` and the same envelope. A new id is minted only when the material content changes, where "material" excludes `submittedAt` and `clientSignals` so a retry is not mistaken for a different request. `SUBMISSION_ID_CONFLICT` is permanent and exhausts the attempt.
- **fix(web): a production build with a real endpoint would have refused every submission.** `NETWORK_ENABLED` was bound to `__E2E_MODE__`, which is false for every `vite build`, so the endpoint compiled in while the network transport was tree-shaken out. The live site would have fail-closed on every inquiry while looking correct in dev and in every test. Found by building against a local stub, not by reading code.
- **fix(web): focus never reached the error alert on the fail-closed path.** A `requestAnimationFrame` scheduled from the same callback that set the state races React's commit, so the alert was still unmounted when the move fired. Now a post-commit effect keyed on a counter, so consecutive failures re-announce.
- **fix(web): the short-pathway topic field had no validation**, so an unmapped empty topic threw out of the click handler and left the form stuck on "sending" with only a console error.
- **chore(infra): the bundle gate now checks the endpoint-enabled build as its own case.** `inspect-bundle.mjs` takes `--expect-endpoint` and asserts both the endpoint and the network transport survive; without one it asserts the transport is absent. The original defect survived precisely because the only bundle check ran against a no-endpoint build, where fail-closed is correct.
- **Memory-only retention.** Nothing is written to `localStorage`, `sessionStorage`, cookies, or the URL; a success clears the envelope. A page reload ends the attempt.
- **Verification: 76 client tests, 23 web wire tests, 421 gas-v2 and 204 V1 tests unchanged.** Mapper output is validated by the **real** `parseEnvelope` loaded into a VM, across all property-type/scope/situation/timing permutations, every topic, and all nine locales. Browser-verified at 390, 834, and 1512 against the dev simulator, a no-endpoint production build, and an endpoint-enabled build pointed at a local stub on `127.0.0.1`.
- **Status: no endpoint exists and none was contacted.** No Apps Script project, Sheet, trigger, deployment, or `.clasp.json`. `scripts/gas`, `scripts/gas-v2`, `apps/qr`, `packages/brand`, and the workflows are untouched. QR Contact Exchange remains unwired and the booking command remains simulated.

## 2026-08-04 (Code Pass 9C: booking eligibility on the success response)

- **fix(gas-v2): `bookingEligible` is forwarded on the HTTP success response.** It was computed once at intake by `isBookablePathway`, stored on the Lead, and returned by `processSubmission`, but `successBody` in `Entry.js` dropped it. A frontend therefore had no way to learn whether to offer booking except by re-deriving the policy itself, which is the competing definition Pass 9B deleted `BOOKABLE_PATHWAYS` to prevent. Found while starting Pass 10A and split out as its own bounded correction rather than worked around.
- **Forwarded, never computed at the boundary.** `Entry.js` returns `result.bookingEligible` and nothing else; a test asserts the file neither calls `isBookablePathway` nor names a pathway literal, so a future edit cannot quietly reintroduce a second policy there.
- **Always a strict boolean, always present.** `true` for Management Proposal at `pm`, `pm_plus_am`, or `undecided` scope; `false` for Investor Services, General Inquiry, and a QR Contact Exchange, which produces no Lead to book against. The Sheet round-trips booleans as strings, so the coercion is explicit: a client branching on `=== true` must not also have to handle `'TRUE'`, `undefined`, or a missing key. A replay returns the same value as the first response.
- **This is an additive response-field correction exposing an existing backend decision, not a new policy.** No storage, tab, intake orchestration, booking execution, email behaviour, or frontend change. **No response schema-version bump was required**, because no V2 frontend and no deployed V2 consumer exists to break.
- **Verification: 421 gas-v2 tests, 204 unchanged V1 tests.** Nine new HTTP-boundary assertions covering each pathway, the QR exchange, replay, boolean strictness, and the no-derivation guard. Lint, type-check, and build pass; V1 `Code.gs` hash and all seven design-archive ZIP hashes unchanged; no change to `apps/`, `packages/`, `scripts/gas`, or the workflows.
- **Status unchanged otherwise: coded and locally tested only.** No frontend connected, no Apps Script project, Sheet, Script Property, trigger, endpoint, or `.clasp.json`, nothing deployed or sent, V1 untouched.

## 2026-08-03 (Code Pass 9B: corrected storage boundary and durable retry recovery)

- **fix(gas-v2): a QR Contact Exchange no longer produces a Lead.** Pass 9A wrote a Lead row for every submission, which made a handshake at a conference into a Lead with an empty pathway, no SLA, and a qualification state nobody would ever set. The model is now six tabs: **Submissions** (one immutable record of every accepted request), **Deliveries** (mutable acknowledgement, notification, and digest state), **Leads** (website service inquiries only), **Contacts** (QR Contact Exchanges only), **Work** (the idempotent side-effect queue), and **Log** (operational history, retained 90 days).
- **feat(gas-v2): every accepted request creates one Submission and exactly one business record.** A service inquiry creates a Lead and no Contact; a contact exchange creates a Contact and no Lead. `buildLead` and `buildContact` throw on the wrong submission kind rather than producing a half-empty row, because a silent empty row is how this defect would return.
- **feat(gas-v2): the Submission tab is insert-only, and that is enforced rather than asserted.** The port declares exactly `insertSubmission` and `findBySubmissionId`. There is no update method on the port, the Sheet adapter, or the test fake, so no caller can write one by accident. Mutable per-submission state lives on the Delivery row precisely so the audit record stays untouched.
- **fix(gas-v2): matching flags and never links.** An exact normalized email or exact full normalized phone records a possible-match flag on the Submission and the business record. It never automatically links, merges, overwrites, or updates an existing Contact, and every QR exchange creates a new Contact. `mergeContact` is deleted; a test asserts it stays gone, and another replaces `updateContact` with a thrower and runs a matching submission through intake, so the rule is proven by the absence of the call rather than the absence of an effect. Two records that turn out to be one person are trivially reconcilable by hand; a merge of two people destroys the losing record's history and nobody finds out.
- **fix(gas-v2): partial-write recovery.** The Submission is written first because it cannot be reconstructed, which means a failure after it left a request half-written: no acknowledgement, no digest entry, nothing in the tab a partner reads, and the retry reported success. A retry now reconciles before returning: a missing Lead or Contact, Delivery row, or work item is repaired idempotently, existing records are never duplicated, and the repair is logged at error level so a silent half-write leaves evidence. Recovery uses the fingerprint-verified retry envelope together with the immutable Submission's recorded identifiers, timestamp, screening result, and match flags, rather than re-deriving them: re-running screening or matching would give different answers because the Contacts tab has grown since. **This is retry-triggered reconciliation, not a background sweep**; a request that is never retried stays half-written.
- **feat(gas-v2): idempotency conflict detection.** `payloadFingerprint` on the Submission hashes the kind, payload, attribution, and locale. It excludes `submittedAt` and `clientSignals`, because an honest retry carries a new clock reading and fill time and treating either as a change would make every genuine retry a conflict. A reused `submissionId` carrying materially different data returns **`SUBMISSION_ID_CONFLICT`** and stores nothing, rather than reporting a replay and silently discarding the new data. **It is an idempotency and conflict-detection guard, not authentication, not authorization, and not a security credential.**
- **Binding client requirement recorded:** any frontend connecting to this endpoint must preserve the same `submissionId` and payload across transport retries. A client that mints a fresh id on retry creates duplicate business records.
- **fix(gas-v2): one booking rule.** `Booking.js` carried its own `BOOKABLE_PATHWAYS` list alongside `isBookablePathway` in Domain.js. Two definitions of one policy drift, and the visible symptom is a form offering a call the command then refuses. The list is deleted; both the intake response and the command call the centralized rule. `bookingEligible` remains stored on the Lead as the intake-time snapshot for the frontend and a future dashboard, and the command re-evaluates the rule against the stored Lead so editing that cell cannot authorise a booking.
- **chore(gas-v2): supporting changes.** `findBySourceSubmissionId` added to the Lead and Contact repositories so reconciliation can ask whether the record for a submission exists; work items keyed on `subjectId` rather than `leadId`, which stopped being true when a QR exchange stopped producing a Lead; the dead `findLeadBySubmissionId` and `listPendingDigestSubmissions` lookups removed because they read columns the Lead no longer has; a deployability test added that rejects duplicate top-level declarations, since Apps Script concatenates every file into one global scope and a second declaration wins or loses by file order.
- **Verification: 412 gas-v2 tests, 204 unchanged V1 tests.** Includes 27 storage-model invariants and 27 partial-write recovery tests that inject a real failure at each of the four write boundaries. `pnpm lint`, `type-check`, and `build` pass; V1 `Code.gs` hash and all seven design-archive ZIP hashes unchanged; no change to `apps/`, `packages/brand`, `scripts/gas`, or the workflows.
- **Status: coded and locally tested only.** No frontend connected, no Apps Script project, Sheet, Script Property, trigger, endpoint, or `.clasp.json`, nothing deployed or sent, V1 untouched.

## 2026-08-02 (Code Pass 9A: approved communications and reconciled backend policy)

- **feat(gas-v2): the approved email system, as one canonical renderer.** `Html.js` (escaping and email-safe primitives), `Labels.js` (wire value to human label), and `Templates.js` (five templates as pure functions). **No embedded-constant-versus-mirror-file duplication**: that V1 pattern kept every template twice and the two drifted silently until a parity test caught it. Each V2 template is one function returning `{ ok, subject, htmlBody, textBody }`, and the plain-text part is written as a specification rather than generated by stripping tags. Built from `AxisPoint QR Contact Emails.dc.html` at the new `design@2026-08-02` package, plus `AxisPoint Communications System.dc.html` and `AxisPointEmail.dc.html` for the Pass 2A shell.
- **feat(gas-v2): QR Contact acknowledgement**, superseding the Pass 8 rule that a Contact Exchange never gets one. Somebody who hands over their details and hears nothing has no way to know it worked or to correct a typo in their own address. It echoes the **display name only**: the submitted address has not been verified as belonging to whoever typed it, so mailing the record back would mail somebody else's phone number to whoever owns that inbox. Phone-only submissions are `skipped`, suspected spam is stored and never acknowledged, and a failed acknowledgement never removes the stored record.
- **feat(gas-v2): the correction and removal promise is gated on configuration.** Its copy says a reply reaches a human who will update or delete a record. With no monitored mailbox and no written procedure behind it that is untrue, so the block is omitted entirely rather than softened, and `configHealth` reports both as named launch blockers. Neither automated reply ingestion nor automated deletion is implemented.
- **feat(gas-v2): conditional daily QR Contact digest at 8:00 AM `America/Chicago`**, replacing immediate per-scan notification. Zero eligible Contacts sends **nothing**, because an empty digest teaches partners to ignore the digest. Routing is by acquisition attribution alone: each partner gets their own card's Contacts, and firm-fallback and unknown-source Contacts go to both in shared sections. Website inquiries keep immediate internal notification. **No trigger is installed**; the handler is callable and unscheduled.
- **feat(gas-v2): digest state is delivery-bound.** A Contact advances to `delivered` only after the entire applicable digest delivered, and a shared Contact only when **both** partners received it. A failure leaves the whole eligible set open, so a retry contains the same Contacts rather than a second copy of a day already read, and a two-day outage produces one honest two-day window line. Digest identity is deterministic over the eligible set. The guarantee is still **bounded at-least-once** and no exactly-once claim is made.
- **feat(gas-v2): the digest splits rather than clipping.** A 90 KB working ceiling per HTML part, splitting at Contact boundaries and preferring group boundaries. A Contact is never split and nothing is truncated. The packer **measures its own rendered output** and re-packs with a larger shell allowance if a part came out oversized: a fixed estimate was wrong in the direction that matters, because the counts panel grows with distinct categories and headings grow with groups. Caught by rendering real specimens, not by review.
- **fix(gas-v2): booking never falsely confirms.** Pass 8 queued the Calendar write and returned `pending` while the visitor's screen said their call was booked; the failure mode is somebody sitting by a phone at 10:30 for a meeting that does not exist. The Calendar port is now called **synchronously** inside the command, which returns `confirmed`, `unavailable`, `rejected`, `failed`, or `not_configured`. `confirmed` is reachable only from a successful `createEvent`. An unreadable calendar returns `failed` rather than being read as available. A Calendar failure never deletes or changes the stored Lead. Booking is Management Proposal only.
- **fix(gas-v2): a QR scan no longer assigns ownership.** `acquisitionSource` is immutable and `ownerPartner` is unassigned at intake for everyone, including a Contact gathered through a partner's own card. A scan gave that partner a name, not a claim, and provisional assignment meant a printed card decided accountability while any reassignment fought a rule that kept reasserting itself. The digest prints "Gathered through" and "Current owner" as two rows, with the owner read live at generation time.
- **fix(gas-v2): identity matching is exact evidence only.** Exact normalized email, or the exact normalized **full** phone digit string. Pass 8's probable and weak suggestions from name, company, and email domain are removed: a suggestion a partner learns to ignore hides the exact match that needed attention. Phone comparison no longer uses the last ten digits, which was a North American assumption that would collide two unrelated international numbers on their tails and merge two people.
- **fix(gas-v2): phone contract is 7 to 20 digits with no country assumption**, restricted to the approved punctuation set, and **stored exactly as entered**. Digits are derived for validation and comparison only. A number reformatted into a shape the person did not write is one they cannot recognise as theirs.
- **fix(gas-v2): one SLA.** Every website `service_inquiry` is due at 5:00 PM `America/Chicago` on the next business day. The pathway-specific 4 / 8 / 24 business-hour policy is removed: three clocks meant nobody could state a deadline without first checking the pathway, so nobody checked. Holidays are ignored at launch, stated as a simplification. Contact Exchange still has no SLA, and only `firstHumanContactAt` satisfies the clock.
- **feat(gas-v2): retention policy, implemented and tested.** Business records never expire automatically and there is no purge job for them; the plan's shape has no key for one, so adding it would be a visible contract change. Operational records (logs, completed or exhausted delivery details) expire at 90 days. **Pending or retryable work is never purged regardless of age**, because an old queued item is a side effect that has not happened yet. Age is measured from completion, not creation. Callable, unscheduled, and supports a dry run.
- **fix(gas-v2): a load-order defect and a shadowing defect**, both caught by tests rather than review. Top-level template style constants read `PALETTE` from another file, which throws for every request whenever Apps Script evaluates the files in the other order; and an HTML helper named `lead` was shadowed by a `lead` parameter in two templates, invoking a Lead record as a function.
- **Additional conflicts found and recorded rather than silently changed:** the firm card had no wire representation (Pass 8 could distinguish only resolved from unresolved, so `acquisitionSource` with `firm` and `unknown` was added), and booking was previously offered on every pathway.
- **Verification: 351 gas-v2 tests, 204 unchanged V1 tests.** Nineteen specimens rendered by the production renderer and checked in headless Chrome at 600, 390, and 320: no horizontal overflow, no visible preheader, no external request, no console error. **A browser is not an email client**: Gmail, Outlook, iOS Mail, and Android have **not** been tested, and this pass claims none.
- **Status after this pass: coded, locally tested, committed, and merged.** No email sent, no digest run, no Calendar touched, no trigger, Sheet, Script Property, endpoint, or Google resource created. Frontends remain unconnected and untouched.

## 2026-08-02 (Code Pass 8: clean V2 backend scaffold and contract implementation)

- **feat(gas-v2): a new, self-contained V2 backend** at `scripts/gas-v2`, sharing no code with `scripts/gas`. Nineteen source files implementing `schemaVersion` 1 of the contract audited in Pass 7, plus 238 tests. **V1 is untouched and remains the deployed backend.**
- **Backend status is `merged` and nothing further.** No `.clasp.json`, Apps Script project, Sheet, Script Property, trigger, or web-app deployment was created; no `clasp push` or `clasp deploy` was run; neither frontend was pointed at anything. No git action advances a change past `merged`.
- **The contract is documented in [`backend-v2-contract.md`](backend-v2-contract.md).** A discriminated envelope on `submissionKind`, UUID primary keys, and a stable snake_case wire vocabulary. Approved display strings are **rejected** as wire values with their own error code, because silently accepting `"Multifamily"` would make the copy deck the contract. Hyphenated frontend union values are rejected for the same reason.
- **Server-owned fields are rejected, not stripped.** Twenty operational field names are refused at any depth in the body, so a submitter cannot set their own SLA or mark themselves not-spam. The one exception is `leadId` on a booking request, where it references an existing lead rather than claiming to set one.
- **Booking is a separate post-submission command.** `payload.booking` inside a submission is rejected outright. An inquiry must be storable while the calendar is down, and a calendar conflict must never be able to reject an inquiry. The calendar write itself is queued, and a superseded booking is abandoned rather than creating an event nobody asked for.
- **Lead and Contact are separate records.** A Lead is a request; a Contact is a person. Matching **suggests and never merges**: exact email or phone links, everything weaker is written to `possibleMatches` for a human, a shared free-mail domain is never evidence, and two conflicting strong candidates produce a new contact plus both suggestions rather than a silent pick.
- **SLA is measured in business hours** (Mon–Fri, 09:00–17:00, project time), 4 / 8 / 24 by pathway, none for Contact Exchange. Satisfied by `firstHumanContactAt` only: an automated acknowledgement proves the machine worked, not that anyone responded. Wall-clock deadlines would mark almost every evening and weekend lead breached before anyone could answer.
- **Spam screening flags and never discards.** Client signals are advisory only and can never clear a content-based flag. A flagged submission is stored, routed to the whole firm for review, and gets no automated acknowledgement.
- **A QR scan identifies which card was scanned, not who owns the lead.** `scannedPartner` and `ownerPartner` are separate fields; ownership from a scan is provisional and freely reassignable. An unresolved slug reaches the firm and is recorded as unresolved rather than attributed to the wrong partner. `refToken` is stored verbatim and **inert by contract**: never resolved, linked, notified, or reported on.
- **Delivery is bounded at-least-once, and the wording is deliberate.** Side effects are queued and run by a trigger so storage never waits on a mail quota; attempts are bounded at 4 with 5/15/60 minute backoff, 20 items per 5 minute cycle. A test deliberately demonstrates a duplicate delivery so nobody later upgrades the claim to exactly-once.
- **`.claspignore` is an allowlist and it is load-bearing.** It denies `**/**` and re-allows only `appsscript.json` and `src/*.js`. A pushed Node test file opens with `require()`, which GAS cannot resolve, and every `doPost` and trigger then throws: a full outage caused by files that are not source. Tests assert the allowlist shape, that no source file uses a Node module system or global, and that `src` loads cleanly **in reverse file order** so nothing depends on Apps Script's evaluation order.
- **No environment value is in the repository.** No project id, Sheet id, deployment id, calendar id, endpoint, or address. Configuration is read from Script Properties by name at call time and fails closed when absent; an unset run mode is `dry_run`, never `live`. Google services are confined to one adapter file, so the suite exercises the real decision code under Node.
- **Deliberately not implemented, and each says so at runtime:** email templates (the port fails **permanently**, the lead is still stored, and `ackEmailStatus` records the failure), Google People sync (`contactSyncStatus: not_configured`, no contacts scope requested), frontend wiring, referral resolution, and origin enforcement (an Apps Script web app cannot read the `Origin` header, so it is documented rather than faked with a setting applied to a client-declared value).
- **chore(infra): `pnpm test:gas-v2` added and wired into the existing GAS workflow** as its own step, leaving `pnpm test:gas` unchanged so a failure names which backend broke.

## 2026-07-31 (Code Pass 6: approved global language selector and locale registry)

- **feat(web): the approved two-slot language selector replaces the static English label** in the header, built from `AxisPoint Language Selector.dc.html` at the new `design@2026-07-31` package. A fixed 82px decorative slot crossfades the native word for "Language" at 130ms on a 1500ms hold, a 1px hairline divider separates decoration from state, and the active locale sits permanently beside it and never animates. No slide, marquee, ticker, bounce, or typewriter motion, and the slot holds a fixed width so the navigation never shifts.
- **feat(web): one canonical locale registry** in `apps/web/src/i18n/locales.ts`, carrying code, native name, native word for "Language", English name, direction, script font stack, line height, translation-review status, and an explicit enabled flag. The cycle and the menu derive from the same registry; the board's rule that "nine locales are never hard-coded into the animation" is honoured literally.
- **Production is English-only and static.** A locale participates only when it is both `enabled` and translation `reviewed`, and English is the only one. With fewer than two available locales the trigger does not cycle, so production ships a still control that advertises nothing unavailable. An unknown or unavailable locale falls back to English.
- **feat(web): development-only preview** at `?locale-preview=all`, gated on `import.meta.env.DEV`. It filters the same registry rather than introducing a second list, cycles all nine proof locales, allows selecting each to inspect the static selected state, and shows a notice stating the page stays in English and that selecting a language translates nothing. Verified against a real production build: the query parameter does nothing, the menu offers one option, and no preview string reaches the bundle.
- **Accessibility.** The cycling slot is `aria-hidden` decoration and never enters the accessible name, which stays "Choose language. Current language: English." The trigger is a button with `aria-expanded`; the panel is a listbox with options carrying a selected state. Enter or Space opens, arrows move, Home and End jump, Enter selects, Escape closes and returns focus to the trigger, Tab closes and lets focus continue so the mobile panel's focus trap is unaffected. Clicking outside closes. The trigger clears 44px.
- **Urdu renders in an isolated RTL run**, so the trigger, caret side, navigation order, and CTA position stay left to right. Verified that the header and document keep `direction: ltr` while Urdu is active.
- **Fonts: production payload unchanged.** Figtree remains the brand utility typeface and the Noto families are language-support fonts, not a third brand face. Simplified and Traditional Chinese use distinct families and are never substituted for one another. No script font is added to the production document, because only English ships; the development preview loads the Noto families on demand instead. Devanagari, Gujarati, Gurmukhi, and Arabic carry the approved 1.55 line height.
- **Verification: 90 of 90 browser checks pass** across 320, 390, 430, 834, and 1512, against both a dev server and a real production build. Coverage includes the approved timing and crossfade-only motion, fixed slot and stable trigger width, pausing on hover, focus, and open, selection freezing the cycle, production staying static, the dev preview cycling, reduced motion staying static, unknown-locale fallback, Urdu isolation, native menu labels, the full keyboard contract with focus return, exactly one visible trigger, the 152px mobile trigger with its locale code, logo/trigger/menu fitting without overlap at every mobile width, and no horizontal overflow. Zero unexpected off-site requests, zero non-GET requests, no console errors. `pnpm lint`, `type-check`, `build`, and `test:gas` (204/204) all pass.
- **Scope note:** component and registry only. No page copy changed, no translations, no locale routing, no `hreflang`, no persistence, no locale stored with a submission, and no change to intake, `apps/qr`, `packages/brand`, or `scripts/gas`.

## 2026-07-31 (Code Pass 5: approved V2 QR frontend; V1 QR app replaced)

- **feat(qr): `apps/qr` rebuilt from `AxisPoint QR Frontend.dc.html`** at `design@2026-07-30`. One scan resolves to one partner, and the visitor never picks a person. The V1 app showed both partners on one page with a combined vCard, an in-page QR generator, a "Share this card" action, and the full V1 `ContactForm` embedded; all of that is gone. The QR JS bundle drops from 260.5 kB to 154.0 kB.
- **feat(qr): approved surface and hierarchy.** Warm `#F6F2EA` field replacing the superseded cool `#F7F5FB`, the shared approved `Mark`, Cormorant only on the name, Save Contact as the one filled teal action, then Call and Email, then three quiet route rows out to the website. No photography, no headshots, no language selector, no form, no in-page QR code, and no desktop composition: the single column caps at a 480px measure and centres.
- **feat(qr): firm fallback** for an unresolved card. Save Contact still prepares the firm card, Email points at the one approved address, no phone action appears because no verified firm number exists, and both partner names appear once as plain text. No error styling and no apology copy, per the board.
- **feat(qr): approved missing-data rules.** A missing phone omits Call entirely and gives Email the full width; a missing partner email keeps Email, routes it to `info@axispoint.llc`, and discloses that in one line. Never a disabled control, never a placeholder, never a masked number. **Partner phone numbers and direct emails are left unconfigured**, because the approved ledger marks them "Needs verification" and they remain open owner decisions. The values in `team.ts`, `utils/vcard.ts`, and the README are V1 and were not treated as approved.
- **feat(qr): Save Contact states** from the board's q7: default, preparing with `aria-busy` and a roughly six-second cap, handoff-mobile, handoff-wide chosen by how the file was delivered rather than by guessing the device, and recoverable failure with a retry and verified-details block. **No state claims a contact was saved**, which is the board's honest ceiling. One live region carries every message, polite for a handoff and assertive for a failure.
- **Simulated and local.** The contact card is built in memory from confirmed values and handed to the browser as an object URL that is then revoked. **No production vCard architecture was selected**: no hosted `.vcf`, no GAS delivery, no client-side library. `prepare()` is the single seam a future decision replaces.
- **No routing contract shipped.** The permanent public profile URL is unresolved and printed on physical cards, so profile selection uses a development-only preview key (`?profile=`), explicitly labelled as not the permanent URL. An unknown or absent key resolves to the firm fallback. The website base URL is configurable through `VITE_WEB_BASE_URL`, defaulting to the local web preview in development.
- **chore(qr): V1 dependencies removed.** The `ContactForm` import, the `qrcode` dependency and its types, the V1 `Logo` component, and the `E2eBanner` are gone. The banner went with the form: the card submits nothing and consumes no endpoint, so an e2e warning would describe a connection that does not exist. `maximum-scale=1.0` was removed from the viewport so the approved 200% text-zoom behaviour is possible, and a real favicon was added.
- **Dead code recorded rather than removed.** No application now imports the V1 `ContactForm`, its steps, or `utils/vcard.ts`; the only references left are inside `packages/brand`. Deleting it was not required for this pass, so it is retained and the cleanup is recorded in `STATUS.md`.
- **Verification: 211 of 211 browser checks pass** across 320, 390, 430, 834, and 1512, covering both partner profiles, the firm fallback, missing-phone and missing-email states, simulated Save Contact success and recoverable failure, the Management Proposal deep link, absence of any intake or QR-generator UI, horizontal overflow, 44px targets, keyboard-only operation with Enter activation, the approved focus ring, heading structure, and reduced motion. Zero non-font off-site requests, zero non-GET requests, no console errors. `pnpm lint`, `type-check`, `build`, and `test:gas` (204/204) all pass. `apps/web`, `scripts/gas`, and `packages/brand` untouched by diff; design archive hashes unchanged.

## 2026-07-31 (Code Pass 4: approved V2 intake frontend; V1 form removed from the web app)

- **feat(web): the approved V2 intake replaces the V1 `ContactForm` on `/contact`.** Built fresh in `apps/web/src/intake` from `AxisPoint Form Design.dc.html` and its `AxisPointFormFlow.dc.html` dependency at `design@2026-07-30`, rather than reskinning the V1 component. The V1 form is no longer imported by `apps/web`; the web JS bundle dropped from 106.8 kB to 89.3 kB. The visible frontend is now entirely V2.
- **Four active pathways.** Management Proposal as the approved three-step flow with the progress ledger, Asset Management as a PM plus AM scope inside that same flow, Investor Services, and General Inquiry as approved one-screen intakes. **Referral Partner and Submit a Referral are deferred for launch scope**: not exposed at the gateway, not built, and none of their V1 role values inherited. A deliberate deviation from the six-pathway inventory, recorded in `STATUS.md`.
- **Asset Management is modelled as a scope, never a pathway or role.** `?intent=asset-management` opens the Management Proposal flow with the PM plus AM involvement answer preselected, and the typed model records `pathway: 'management-proposal'` with `scope: 'pm-plus-am'`. The Asset Management page CTA was moved off the temporary property-management-only token.
- **feat(web): typed frontend draft model** in `intake/model.ts`, separating pathway, service scope, property and portfolio detail, situation, timing, contact, follow-up language, referral code, and booking state. It carries no V1 role values, no `qualData` bag, and no payload builder. Open backend decisions are listed there rather than invented.
- **Submission is simulated and local.** No fetch, no XHR, no form action, no endpoint. Booking uses clearly labelled local fixture availability with a simulated confirmation, and no calendar request leaves the browser. `?ref=` is held in local draft state and never transmitted. Verified: **zero non-font off-site requests and zero non-GET requests across the whole run.**
- **States implemented from the approved sources:** empty, filled, required-field validation with inline errors and a focus-managed summary alert, disabled actions, loading, recoverable failure with retry, simulated success, booking available, booking confirmed, booking skipped, and follow-up language selection. Development-only state inspection is available through `?state=…` and a `fail@` email, both gated behind `import.meta.env.DEV` so neither reaches production output.
- **Accessibility:** native controls throughout, no component library added to reproduce a field. Choice groups are real radio groups inside `fieldset` and `legend` sharing one name, so arrow keys move the selection. Verified behaviourally: **Enter and Space both activate the gateway with no pointer fallback**, a choice row takes focus by Tab alone, the approved 3px `#38285D` ring is visible on it, and ArrowDown moves the selection. 15 of 15 keyboard checks pass.
- **chore(web): V1-only CSS removed** now that nothing references it: the `.rv` / `.d1`-`.d5` scroll-reveal utilities, the `@media (max-width: 767px)` `!important` override block that re-tuned V1 page-body classes, and the unused `.tab-scroll` and `.scrollbar-thin` helpers.
- **fix(web): `html, body { overflow-x: hidden }` removed.** It concealed real overflow and made automated checks unreliable, since `scrollWidth` can never exceed the viewport when overflow is clipped. Pass 3 hit exactly that. Overflow is now detectable, and every route was re-verified clean at 1512, 834, and 390 with it gone.
- **fix(web): `/contact` had two `h1` elements** once the intake mounted, because the approved page hero and the approved intake screens each carry one. The intake screen titles are now `h2` and the step sub-heading `h3`, leaving the page hero as the single `h1`.
- **Note for future verification: `/share/:code` must not be navigated during local browser testing.** It is V1 code that hard-redirects to `https://axispoint.llc` via `window.location`, so visiting it sends real requests off localhost. It is untouched in this pass and was verified by diff only.
- **Verification:** 84 of 84 intake checks and 15 of 15 keyboard checks pass across 1512, 834, and 390. `pnpm lint`, `type-check`, `build`, and `test:gas` (204/204, suite unchanged) all pass. `apps/qr`, `packages/brand`, `scripts/gas`, and `SharePage.tsx` are untouched by diff. No deployment, `gas:push`, `clasp push`, or `clasp deploy`.

## 2026-07-31 (Code Pass 3: approved V2 public pages and routes)

- **feat(web): the six approved V2 routes replace the V1 public pages.** `/`, `/property-management`, `/asset-management`, `/investor-services`, `/partners`, and `/contact`, built from `AxisPointPage.dc.html`, `AxisPoint Property Management.dc.html`, and `AxisPoint System Studies.dc.html` at `design@2026-07-30`. `/services` collapsed three approved pages into one and is gone; `/team` is now `/partners`. No redirect was added for either, since nothing in this repository is deployed. `/share/:code` and its referral behaviour are untouched.
- **feat(web): Property Management gets its dedicated page** with the approved seven-section structure intact: hero, photograph band, what AxisPoint takes responsibility for, the four operating functions, where AxisPoint is strongest, the questions owners ask, the related strip, and the closing band. Multifamily and retail stay primary property types; the Texas MSA, onsite-staff, portfolio, lease-up, heavy-capex, transition, and turnaround positioning is carried over verbatim. Its authoritative source is used per `design-sources.md` correction 2, and no retail photograph was added because that source uses none.
- **feat(web): Asset Management is presented only as the optional PM plus AM strategic layer**, with the approved two-layer comparison, and Investor Services keeps its smaller separate positioning and its own "Discuss an acquisition" action.
- **feat(web): Partners** uses only approved copy. Both people are titled Partner, with no headshots, no external profile links, and no invented biographies, credentials, phones, or metrics. The longer V1 biographies in `team.ts` are deliberately not used: they are V1 copy.
- **feat(web): licensed photography shipped as responsive derivatives.** Owner confirmed Adobe #158947695, #196537616, and #110458363 on 2026-07-30; #04 is the cleared Juan Nino Unsplash asset. 48 derivatives across AVIF, WebP, and JPEG at four widths, with `srcset`, explicit dimensions to prevent layout shift, lazy loading everywhere except the homepage hero, and the approved per-viewport focal points. Band images are pre-cropped to 2:1 because their slots are roughly 400px tall. Originals, watermarked studies, and the uploads tree are not committed. Catalogued in the new `docs/asset-catalog.md`.
- **feat(web): per-route SEO metadata.** A small `useDocumentMeta` hook, not a new dependency, sets a unique title and description, a canonical URL, and Open Graph title, description, url, and type for each route. No `og:image` is emitted, because the 1200x630 image does not exist yet and referencing a missing file is worse than omitting the tag. No structured data was added: nothing on these pages carries a verified address, phone, rating, or metric to support it.
- **fix(web): the homepage rendered two `h1` elements.** The desktop and mobile hero compositions were duplicated markup. They are now one DOM tree whose band is repositioned with flex `order`, so the page has exactly one `h1` and one photograph element. Only the non-interactive image moves, so reading and tab order still match what is seen.
- **fix(web): desktop navigation targets were under 44px.** Nav links measured 20px and the header action 42px. The Pass 2 audit missed this because its touch-target check only ran at 390px, where the desktop links are hidden. Padding with equal negative margins grows the hit areas while keeping the approved header height.
- **fix(web): the Contact panel rendered completely blank.** `ContactForm`'s default class string begins with `rv`, the V1 scroll-reveal class that sits at opacity 0 until the `useReveal` observer added `.in`; that hook belonged to the V1 pages removed in this pass. The fix passes the component's existing `className` prop, the same integration point `apps/qr` already uses. **Nothing inside the form was modified.** The verification check that had passed while the panel was blank now asserts the intake is actually painted.
- **Verified in a real browser** across all six routes at 1512, 834, and 390: **236 of 236 checks pass**, covering approved copy and heading structure, one `h1` per page, landmarks, photograph loading and lazy behaviour, CTA destinations, canonical and Open Graph metadata, 44px targets, horizontal overflow, console errors, em dashes, the removal of `/services` and `/team`, `/share/:code` remaining outside the chrome, and the intake rendering visibly. `pnpm lint`, `type-check`, `build`, and `test:gas` (204/204) all pass.
- **Temporary, recorded in `STATUS.md`:** the Contact shell is V2 while the intake inside it remains V1 until Pass 5, and the Asset Management action routes to `intent=property-management` because no PM plus AM scope value exists yet. No backend role was added and the intent mapping is unchanged.

## 2026-07-30 (Code Pass 2: approved V2 shared frontend foundations)

- **feat(brand): `Mark`, the approved AxisPoint lockup and mark-only component.** Geometry copied exactly from `AxisPointMark.dc.html` at `design@2026-07-30`: same `0 0 64 85.45` viewBox, same `translate(-17.44 -11.66)` transform, same eight shapes in order. Five palettes: the four approved variants plus `onDark`, which `AxisPointFooter.dc.html` specifies for the `#141020` field. The wordmark is live Figtree text rather than a vector outline. Replaces the previous `Logo`, which approximated the mark with a 60%-opacity purple and carried a vector "CRE Asset Management" tagline reflecting superseded asset-management-first positioning.
- **feat(brand): approved V2 design tokens, added additively.** `v2-surface` `#F6F2EA`, `v2-ink`, `v2-teal`, `v2-teal-support`, `v2-purple`, `v2-magenta`, `v2-action-label` `#0F1F27`, `v2-footer` `#141020`, a 2px `v2` radius, and a 1240px `v2` content measure, in both the Tailwind preset and `colors.ts`. **No existing token value was changed**, because `apps/qr` consumes the same preset and this pass does not restyle that surface. `#F6F2EA` supersedes the older cool `#F7F5FB`, which the approved QR board records as inherited from an older brief.
- **feat(web): shared header rebuilt to the approved source.** Four service destinations, the approved static English label, and one filled teal action carrying `#0F1F27` at weight 700 on a 2px radius, which is the only filled element in the navigation. The language control is deliberately **not interactive**: no button, no `aria-haspopup`, no tab stop, because the selector's real behaviour belongs to the localization pass and a control that opens nothing would be fabricated. Desktop collapses to the approved menu below 1024px; the approved sources define no tablet composition and the four labels plus the full CTA wording cannot hold their measure at 834px.
- **feat(web): mobile menu meets the keyboard contract.** Accessible name, `aria-expanded`, opens and closes by keyboard, Escape closes, focus moves into the panel on open and returns to the trigger on close, and Tab and Shift+Tab are trapped so focus cannot land behind the open panel.
- **feat(web): shared footer rebuilt from `AxisPointFooter.dc.html`** with its real identity, Services, Firm, and Houston/contact structure. Nothing invented: no street address, phone number, metric, testimonial, certification, client relationship, or social link.
- **feat(web): shared layout shell** with a working skip link as the first tab stop, a `main` landmark, the approved surface, and a `site-footer` anchor.
- **feat(web): approved global treatment.** Figtree becomes the default for headings, with Cormorant Garamond reserved for editorial display moments that opt in. The previous rule made Cormorant the default for every heading, inverting the approved system. Adds the approved focus ring (3px `#38285D` at 3px offset) and a `prefers-reduced-motion` block.
- **feat(web): the real `favicon.svg` now exists**, built from the approved mark-only geometry. `index.html` had referenced a file that was never present. Figtree 700 added to the font load, which the approved weights require.
- **fix(web): 44px hit area on the header lockup.** The approved lockup is 23px tall. Vertical padding with an equal negative margin grows the target while keeping the header at its approved 63px height. Caught by browser verification, not by review.
- **Verified in a real browser** at 1512, 834, and 390: 20 of 20 checks pass, covering skip link, focus ring, menu keyboard behaviour, focus trap and return, touch targets, horizontal overflow, console errors, and em dashes. `pnpm lint`, `type-check`, `build`, and `test:gas` (204/204) all pass.
- **Scope note:** page bodies were not rewritten and no route was added or removed. The navigation and footer link to four approved routes that arrive in Pass 3; those are recorded in `STATUS.md` rather than hidden behind placeholder pages. No change to `apps/qr`, `scripts/gas`, intake, payloads, or booking. No deployment or external mutation.

## 2026-07-30 (Code Pass 0: workflow and source-of-truth reconciliation; V2 design manifest)

- **docs(infra): `AGENTS.md` replaced with a tracked pointer to `CLAUDE.md`.** It was previously untracked and a full second copy of the workflow rules, and it had drifted: it still asserted "there is no staging gate yet. Every merge currently ships toward production" after the 2026-07-23 audit disproved that and `CLAUDE.md` was corrected. Two complete copies of the same rules drift, and the stale one is the one somebody eventually follows. There is now exactly one copy, plus a table saying which file owns which topic.
- **docs(infra): rule ownership reconciled across `CLAUDE.md`, `docs/branching.md`, and `docs/deployment.md`.** `CLAUDE.md` keeps terse operative rules and delegates; `branching.md` is the single full home for branch rules, the merge model, auto-merge/hold criteria, and tags; `deployment.md` owns IDs, push-vs-deploy mechanics, and external mutations. The duplicated "corrected model" and GAS-deployment prose in `CLAUDE.md` was condensed to pointers.
- **docs(infra): auto-merge rule clarified.** Routine, self-contained, completed code merges normally once checks pass, explicitly including locally tested GAS code. A PR is held open in exactly two cases: an unresolved product or backend-contract decision, or a task requiring an unauthorized external action. Replaces the prior "no carve-out exceptions" absolutism, which sat in tension with the same file's external-mutation boundaries. **A merge still never implies `clasp push`, `clasp deploy`, resource creation, or any external mutation.** Added an explicit backend status vocabulary (coded → locally tested → committed → merged → pushed to HEAD → deployed → verified).
- **docs: `docs/design-sources.md` added — the bridge between the approved design package and the code.** Records `design@2026-07-30`, the external read-only archive location, the authoritative source and route tables, the required-dependency graph, the historical/non-authoritative list, the QR seven unresolved values, and the photography licence ledger. Also records **four corrections to the exported Design Index**, whose classifications are stale: `AxisPointPage` is authoritative for the homepage only; `AxisPoint Property Management` is authoritative for `/property-management` (not historical); `AxisPointFooter` is the required shared footer dependency (not historical); `AxisPoint System Studies` is authoritative for Asset Management, Investor Services, Partners, and the Contact shell. The archive is external and was not modified.
- **docs: `docs/STATUS.md` added** — approved design version, current pass, deferred features, open owner decisions, known risks, and rollback anchors. Records the rollback anchor as **`pre-v2-clean-rebuild`** at `d194e7e` (not `v1-pre-rebuild`, since that baseline already contains early V2 work). The tag was **not** created.
- **docs: `README.md` rewritten** for Property-Management-first V2 positioning, Asset Management as an optional PM plus AM scope rather than a separate service or role, Investor Services as a separate smaller pathway, the approved six-pathway intake model, and accurate deployment status. It previously led with asset-management-first positioning, described the V1 five-lead-type model and the pre-unified 11-tab/31-column schema as current, and described FTP deployment as working.
- **chore(infra): CI now runs `pnpm type-check` and `pnpm lint`** in addition to the two app builds. Lint had never run in CI. Note that only `apps/web` currently defines a `lint` script.
- **chore(infra): `scripts/gas-v2/.clasp.json` added to `.gitignore` before the directory exists.** The existing rule is a literal path, not a pattern, so a new GAS project's script ID would otherwise be committed on its first `git add`.
- **docs: the `qualData` persistence warning in `frontend-payload-schemas.md` marked HISTORICAL.** It described "12 of 13 fields collected and never stored" in the present tense alongside a forward-looking "the migration will fix this" note, but the unified-schema cutover had already shipped (`USE_UNIFIED_SCHEMA = true`, `Code.gs:1348`, PR #47), so all 13 fields persist into the `Details` blob. Marked rather than deleted, because the legacy branch still exists behind the flag.
- **docs: the FTP mirror-delete open question in `deployment.md` resolved.** Neither deploy workflow passes `dangerous-clean-slate`, and the action does not mirror-delete by default, so deploys add and overwrite but never remove stale files. Recorded with the two options for cutover.
- **Scope note:** documentation, CI, and `.gitignore` only. No change to `apps/web`, `apps/qr`, `packages/brand`, or `scripts/gas`. No Apps Script push or deploy, no Google resource, hosting, DNS, or secret change. Verified with `pnpm lint`, `pnpm type-check`, `pnpm build`, `pnpm test:gas`.

## 2026-07-23 (safe/intentional dev-mode split; workflow model corrected; `deploy:gas` → `gas:push`)

- **feat(web/qr): mode-driven, fail-safe form-endpoint resolution.** Each app's `vite.config.ts` now decides the endpoint by Vite mode and injects it via a `__FORM_ENDPOINT__` define (the shared `<ContactForm>` no longer reads `import.meta.env.VITE_FORM_ENDPOINT` directly). `pnpm dev` (mode=development) **forces the endpoint empty** — any `VITE_FORM_ENDPOINT` from `.env.local`, the shell, or any generic env source is ignored, so the simulated-success fallback always runs and no request reaches the real backend. `pnpm dev:e2e` (mode=e2e) loads the real endpoint **only** from the gitignored, machine-local `.env.e2e.local` and **fails loudly** (throws, no silent fallback) if it is missing, printing a loud terminal warning and rendering a fixed in-app red banner (`E2eBanner`). Added `dev:e2e` / `dev:e2e:web` / `dev:e2e:qr` scripts (root + per app), using Vite `--mode` (cross-shell, no POSIX `VAR=value` syntax). Production builds still take the endpoint from the build environment; a build with none supplied compiles in no endpoint and does not inherit a cached one. Verified by build-artifact inspection across dev/e2e/production modes (E2E wiring only: real endpoint compiled as the `fetch(..., POST)` target + payload builder wired — not proof of GAS side effects).
- **chore(infra): the production endpoint was removed from both `.env.local` files** and relocated to gitignored `.env.e2e.local`; tracked `.env.e2e.example` placeholders added. The stale `dist/` builds (which had the real endpoint baked in) were rebuilt clean. Note: the `/exec` URL still appears in tracked `docs/deployment.md` and `scripts/gas/Code.gs` by design (it is a public anonymous-access Web App endpoint) — flagged, no history rewrite performed.
- **chore(infra): `deploy:gas` renamed to `gas:push`** — the old name implied a production release, but it only mutates Apps Script HEAD. README and docs references updated.
- **docs: workflow model corrected (`CLAUDE.md`, new `docs/branching.md`, `docs/deployment.md`).** Prior assumption "`main` = production / a merge ships toward live" was disproven by direct audit (deploy workflows 69/69 failed at the FTP step since April; live sites are a separate hand-uploaded build; FTP secrets unset). Merging deploys nothing until FTP secrets are deliberately added later. Backend `gas:push` and `clasp deploy -i` are separate, deliberate operations, neither part of "done." Forward-looking guidance only — historical CHANGELOG / migration-plan entries describing what past releases required are left intact.

## 2026-07-20 (daily digest → branded HTML; digest Source/Heard-About field fix; full template variable audit)

- **feat(gas): the daily digest is now a branded HTML email.** Root cause of the "unstyled plain text" report: `sendDailyDigestUnified` built a plain string and called `GmailApp.sendEmail(to, subject, body, {name})` with **no `htmlBody` and no template** — it never used one of the `emails/*.html` templates at all. Added an **8th** template, `TEMPLATE_DAILY_DIGEST` + `emails/daily-digest.html` (CID logo, brand color bar, Arial body — matching the existing 7; email clients don't render web fonts, so no template uses Cormorant/Figtree), and a JS `buildDigestLeadCard` for the variable-length `{{leadBlocks}}`. Sends with `htmlBody` + inline `logo`; the plain body is now a one-line fallback. The dormant `sendDailyDigestLegacy` path is untouched (still plain text).
- **fix(gas): the digest's blank "Source" for Investor / RE Pro / Referral Partner.** Not a stale key name, but the schema-level version of one: the old digest surfaced **only the `Source` column** (arrival channel — `QR`/blank, and blank for every direct web submission), and never the **`Heard About` column**, which is where the values the user saw in the Sheet ("LinkedIn", "Personal referral") actually live. `Source` and `Heard About` are two deliberately-separate columns (see *`source` vs `heardAbout`*). The digest card now shows **both**: `Source` (blank→`Direct`, or `QR`) and a separate **`Heard about us`** row from `Heard About`, **omitted when blank**. EAO collects no Heard About, so it correctly renders no such row — that blank is by design, not a bug.
- **Full variable audit across all 8 templates — no other field bugs.** Cross-checked every `{{placeholder}}` in each mirror against the vars each send function passes: `partner-notification`, `referrer-notification`, `referrer-monthly-summary`, the three visitor confirmations, and `welcome-subscriber` are all correctly wired (e.g. `partner-notification` already reads `leadSource()` for its `Source` row and `leadHeardAbout()` for a separate `Heard about us` row — the same correct split the digest was missing). The only field defect was the digest's.
- **Verified beyond tests:** rendered the real `TEMPLATE_DAILY_DIGEST` from the real `Code.gs` against one lead of **every** type (Investor, RE Professional, Referral Partner, Existing Asset Owner, plus a QR-sourced lead), captured the `htmlBody`, and inspected it in a browser: logo header + "Daily digest" badge, per-lead cards, Investor/Pro/Referral showing `Source: Direct` **and** their `Heard about us` value, EAO showing `Source: Direct` and **no** Heard-about-us row, QR lead showing `Source: QR`. Suite **204/204** (+ a new digest test pinning the Source-vs-Heard-About behavior across types, incl. the EAO omission). **Requires the manual `clasp push` + `clasp deploy -i <prod id>` step to reach the live endpoint — a merge ships nothing.**

## 2026-07-19 (two live-tested bug fixes: email emoji encoding + calendar guest display names)

- **fix(gas): the `visitor-no-booking` calendar icon rendered as `❓❓` in delivered email — root-caused to an astral-plane encoding downgrade, not a corrupt file.** The `📅` (U+1F4C5) shipped as valid, byte-identical UTF-8 in both the embedded constant and the mirror, yet arrived as two `?`. Cause: it is a UTF-16 **surrogate pair**, and the `clasp push` → Apps Script → `GmailApp` path downgrades supplementary code points, emitting one `?` per surrogate half (BMP symbols `→ · ⚠ ✓` in the same templates are unaffected). **Fixed at the root** by replacing the raw emoji with the ASCII entity `&#128197;` in both copies, so the byte stream is pure ASCII and nothing in the pipeline can re-corrupt it on the next push. See `email-templates.md` → *Encoding gotcha*.
- **fix(gas): calendar invites now show the guest's submitted name, not a Google-guessed one.** Attendees were added as `{email}` only, so Google rendered the visitor's name from the address local-part (`zach+testpro@axispoint.llc` → "Zach+Testpro"). The visitor is now added as `{email, displayName}` with `displayName` = the submitted `firstName`/`lastName`. The advanced Calendar Service was **already enabled** (`appsscript.json`) and already the primary insert path, so no service change was needed; the `CalendarApp` fallback stays email-only (it cannot set a display name — the reason the advanced insert is preferred). Partners (our own accounts) get no invented display name.
- **Verified against the real `Code.gs`, not just the diff:** rendered the actual `TEMPLATE_VISITOR_NO_BOOKING` (icon emits `&#128197;`, decodes to 📅, zero astral chars remain) and drove the real `createBookingEvent` with a captured `Calendar.Events.insert` (visitor attendee = `{email, displayName:"Zach Testpro"}`, partners email-only, event healthy). Full suite **202/202**. **Requires the manual `clasp push` + `clasp deploy -i <prod id>` step to reach the live endpoint — a merge ships nothing.**

## 2026-07-16 (admin actions + EAO visitor-note removal; two "already done" items verified, not rebuilt)

- **feat(gas): the four menu-callable admin actions now exist** — `setLeadStatus(leadId, newStatus)`, `setReportsEnabled(leadId, enabled)`, `forcePartnerSummaryNow()`, `forceDailyDigestNow()` — built against the live unified schema and added to the **AxisPoint** menu. **Menu-callable only, deliberately:** each mutates or emails from live CRM data and `doPost` is unauthenticated (it must be, the contact form is anonymous), so an API-callable pass is a separate change that has to bring auth with it. Each is split into a plain-argument operation (editor/trigger/test-callable) and a UI-only wrapper, because `getUi()` throws outside a bound context. **Requires the manual `clasp push` + `clasp deploy -i <prod id>` step to reach the live endpoint — a merge ships nothing.**
- **The Contacts side effect was extracted, not duplicated** (`applyStatusContactSideEffect`). An installable `onEdit` trigger **does not fire for a write made by Apps Script**, so `setLeadStatus` cannot rely on `handleStatusEditUnified` running after its write; without an explicit call, a menu-driven status change would update the Sheet while silently skipping the Contacts move an identical hand-typed edit performs. Both paths now share one body.
- **`setReportsEnabled` takes a `leadId`, deviating from the requested single-arg signature — on purpose.** Reports Enabled is a per-partner choice, so a global version would mass-write every Referral Partner row and destroy each partner's individual opt-out: a bulk mutation of live data no git revert undoes. It also writes real booleans, because the summary sender's rule is asymmetric (only an explicit `FALSE` opts out), so storing `''` would read back as **enabled** and the opt-out would silently fail. Flag coercion goes through `normalizeEnabledFlag`, not `Boolean()` — a prompt supplies the string `'false'`, and `Boolean('false') === true` would enable a partner the user just asked to disable.
- **fix(gas): the EAO "What you told us" callout is removed from the client confirmation.** It was **not** a section in any of the 7 HTML templates — it was one of five per-role labels in `buildVisitorPersonalNote`, rendered into the `{{personalNote}}` placeholder all 3 visitor templates carry, and the string was **EAO-only**. EAO now renders no callout; the other four roles are untouched. No template file changed, so the embedded-constant/mirror pair cannot drift here. **This removed an ECHO, not a field:** `pressing_issue` still persists to `Details.pressing_issue` and still reaches the internal surfaces via `leadMessageText()`; tests pin both.
- **docs(gas): two stale `Code.gs` comment blocks corrected** — the header still read *"MIGRATION STATE (Stage 1 of N) … the single switch, and it is off"*, and the switch comment still carried a *"that manual run has NOT happened, do not deploy"* warning for `setupSpreadsheetUnified()`. Both were overtaken by the 2026-07-15 cutover and the 2026-07-16 Phase A confirmation; `/docs` had already flagged them.
- **⚠️ TWO REQUESTED "FIXES" WERE ALREADY DONE — verified against source, not rebuilt.** The task described them as live data-loss bugs; both descriptions were **stale, and matched the LEGACY row builder** (now the rollback path only), not the live unified path. **(1) qualData persistence:** all 12 named fields (plus `assetClasses` = 13) already persist to `Details` via `LEAD_TYPES.detailsFields` → `buildLeadDetails`, delivered by the migration's Stage 6. **(2) Total Downstream:** `creditReferralChain` already walks `chainAncestors(chain)` and credits **every** ancestor, keeping `Direct Referrals`/`Last Referral Date` on the immediate referrer only — with an existing passing test, *"a 4-deep chain credits EVERY ancestor exactly +1."* No code was changed for either; rewriting working, tested code to satisfy a stale premise would have been the larger risk. Recorded here because the same stale audit will otherwise be re-run.
- **docs(infra): `frontend-payload-schemas.md` EAO section corrected** — it still claimed `normalizeEaoPayload` synthesizes `message` from `pressing_issue` and stuffs `preferences` with a JSON dump. **Both were removed by the 2026-07-15 A1/A2 cleanup and the doc never caught up**; that staleness predates this task.
- **Suite: 202/202** (+30 in a new `tests/admin-actions.test.js`, including a guard that every function name `onOpen` registers as a string actually exists — a misspelling there is a menu item that throws only when a human clicks it). **Verified beyond tests:** the real confirmation email was rendered from the real `Code.gs` for all 5 lead types across all 3 template variants. **Not verified live:** nothing here has been driven against the real Sheet or the production endpoint — the `clasp` token cannot read Sheets, and the menu items are observable only in the bound spreadsheet after a deploy.

## 2026-07-16 (unified-schema cutover: Phase A DONE, Phase C partially confirmed live)

- **docs(infra): record the user-verified cutover confirmation** so it no longer relies on conversation memory. Not verifiable from the repo (the `clasp` token cannot read the live Sheet); confirmed manually by the user on this date.
- **Phase A — `setupSpreadsheetUnified()` — DONE.** Run by hand from the Apps Script editor; execution log reported **"Leads + Referrals + Subscribers ready (3 tabs)."** The `Leads` tab exists, so migrated functions are not throwing on a missing table. (The pre-cutover "has NOT happened" warning still in the `Code.gs` switch comment is now stale.)
- **Phase C — live submission verification — PARTIAL (2 of 5 lead types).** Real **Investor** and **Existing Asset Owner** submissions through the **actual production endpoint** each produced a correct `Leads` row (right values in the right columns, correct `Details` JSON structure) and correctly triggered both the visitor confirmation and internal notification emails. The EAO submission specifically confirmed the **A2 cleanup live**: `Details.pressing_issue` populated, `Details.message` empty (no duplication).
- **Still OPEN:** the other three lead types (`pro`, `referral`, `submit_referral`) have not been driven live, and the **§8 live-concurrency checks have NOT been run** — real mutual exclusion under load remains observable only live, so unproven until then. Tracked as *Known open defects* #6 in `backend-architecture.md`.

## 2026-07-15 (unified-schema migration: EAO normalizer cleanups A1 + A2 — post-cutover, live-affecting)

- **fix(gas): remove the two EAO `normalizeEaoPayload` items the Stage-6 audit left open (A1, A2).** The `USE_UNIFIED_SCHEMA` switch is now live, so this was treated as a real production change (clasp push + `clasp deploy -i` + a live EAO re-test required after merge), not a tidy-up.
- **A1 — the `preferences` JSON-string hack.** `normalizeEaoPayload` no longer sets `payload.preferences = [eaoDetailsSummary(payload)]`, and the exact-value filter in `buildLeadDetails` that existed only to strip it is deleted. Confirmed against current code that all eight EAO detail fields (`portfolio_type` … `pressing_issue`) already have real `Details` keys via the registry (`detailsFrom: 'payload'`), so nothing is lost. **Behavior change beyond the Sheet, which the "harmless" framing had missed:** `createContact` writes `payload.preferences` into the Google Contact's "Preferences:" note, so EAO contacts previously carried the raw JSON blob there; that note line is now empty (the data lives in the Sheet's `Details`). `eaoDetailsSummary` is retained with its unit test as the reference pattern but is now wired into nothing — a cutover-cleanup deletion candidate.
- **A2 — `Details.message` duplicating `Details.pressing_issue`.** `normalizeEaoPayload` no longer sets `payload.message = payload.pressing_issue`, so `Details.message` is now `''` for EAO and the free text lives once, in `pressing_issue`. **Not a two-line deletion:** EAO's `payload.message` was the ONLY path `pressing_issue` reached four internal-email surfaces (partner-notification Message block, internal booking dump, and both resubmission notices), so a naive removal would have dropped it from the internal email. Fixed holistically with a new `leadMessageText(payload)` helper — returns `payload.message` for every role unchanged, falls back to `pressing_issue` for EAO (mirroring `buildVisitorPersonalNote`, which already read it directly) — and routed those four surfaces through it. Storage is deliberately NOT routed through the helper, so the redundant stored copy stays gone.
- **Verified end-to-end**, not just by tests: a real EAO submission + resubmission driven against the actual `Code.gs` confirms `pressing_issue` still surfaces in the Sheet, the internal email HTML, the booking dump, the visitor note, and the resubmission email, while `Details.message` is empty. **Suite: 172/172** (added a `leadMessageText` unit test; updated the two tests that asserted the old hack/filter behavior).

## 2026-07-15 (unified-schema migration CUTOVER: switch flipped in code — Phase B step 4)

- **feat(gas): `USE_UNIFIED_SCHEMA` flipped `false` → `true`** (`Code.gs`). One-value change; this is §8 Phase B step 4, the repo side of the cutover. Every dispatcher now routes to its `xxxUnified` body. **No legacy code deleted** — that is Phase D, later and separate.
- **This ships NOTHING on its own.** GAS goes live only via manual `clasp push` + `clasp deploy -i <prod id>`; a merge deploys nothing. As of this commit **nothing is deployed and the unified path is not live.**
- **⚠️ ORDERING — the manual steps are still pending and MUST run in §8 order:** (1) §8 **Phase A** — `clasp push`, then run **`setupSpreadsheetUnified()` by hand** from the Apps Script editor to create the `Leads` tab. **This must happen before the deploy** — with the switch `true` and the tab absent, every migrated function points at a table that does not exist and throws. (2) §8 **Phase B step 5** — deploy. (3) §8 **Phase C** — verify one live submission per lead type + the live-concurrency checks. Rollback at any point: set the switch back to `false`, push + deploy; legacy tabs and bodies are intact until Phase D.
- **Test hygiene surfaced by the flip (necessary for a green cutover PR):** two pre-dispatcher test files — `live-sheet-functions.test.js` and `pure-functions.test.js` — asserted **legacy** behavior but never set `USE_UNIFIED_SCHEMA`, relying on its module default being `false`. Flipping the default routed their `buildLeadRow`/`moveColdLeads`/`findExistingLead`/`onSheetEdit` calls to the unified path and failed 5 tests. **Not a real regression** — the unified behavior is correct (e.g. `submit_referral`'s referred person now lands in `Details.referred`, not a Message prose block). Fixed by pinning both files to the legacy branch explicitly (`USE_UNIFIED_SCHEMA = false`), exactly as every newer test file already does; these files are deleted/rewritten at Phase D with the legacy bodies they exercise. Test-only; no production code touched beyond the one-value flip.
- **Suite: 171/171 green with the switch flipped.** The switch comment in `Code.gs` was updated to reflect the flip and to carry the "run `setupSpreadsheetUnified()` before deploying" ordering warning inline.
- **NOTE ON NAMING:** the task that drove this called it "Phase A," but per §8 the switch flip is **Phase B step 4**; §8's Phase A is the manual tab creation, which is still pending. The plan's execution-status and §8 intro are updated to reflect the true state.

## 2026-07-14 (unified-schema migration STAGE 9: setup — ALL 9 STAGES DONE, cutover-ready)

- **refactor(gas): Stage 9 adds `setupSpreadsheetUnified` — the last stage before cutover.** With it, the migration is **code-complete**: every lead-data function is migrated (Stages 1-8) and the setup path that makes the `Leads` tab exist now exists too. `USE_UNIFIED_SCHEMA` is still **off**; nothing has changed in production.
- **`setupSpreadsheetUnified` is a NEW, separate function — deliberately NOT a dispatcher and NOT a rewrite of `setupSpreadsheet`.** Reasoning (the task invited judgment here): it is a **manual admin action** run by hand from the Apps Script editor, never a request path — and there is a **chicken-and-egg with the switch**. The `Leads` tab must exist *before* `USE_UNIFIED_SCHEMA` flips (a migrated function pointed at a missing tab throws), so a switch-gated setup would create the **legacy** tabs while the switch is still off — exactly backwards. A separate name makes the cutover order unambiguous: run this, *then* flip. Legacy `setupSpreadsheet()` is **untouched** and still creates the 11 legacy tabs until cutover.
- **It creates exactly `Leads` + `Referrals` + `Subscribers`.** `Leads` on the 25-column `UNIFIED_LEAD_HEADERS` (`Details` last); `Referrals` and `Subscribers` on their **own unchanged** schemas — re-confirmed against §1, they were never part of this migration. The empty-tab guard (`getLastRow() === 0`) is kept, so it never touches a populated tab and is safe to re-run.
- **FINDING — `.tab`/`.tabColor` do NOT come out of the registry yet, and the task's own verification step is why.** "Confirm nothing else still reads them" was the instruction; the honest answer is that **many things do**: every legacy body (`persistNewLeadLegacy`, `onSheetEditLegacy`, `updateReferrerStatsLegacy`, legacy `setupSpreadsheet`) and every §4 delete-at-cutover function (the header audit/repair family, `eaoBackfillPlan`, `categoryTabForRole` → `setCategoryTabStatus`). All survive until cutover as the rollback path, so removing the fields now would break the legacy branch the staging pattern requires to stay byte-for-byte intact. **Their removal is a cutover line item, not a Stage 9 change** — and a test asserts they are still present so an early cleanup fails loudly rather than silently breaking the legacy tests. This is the fourth time the dependency graph has overruled a "just remove/retarget this" instruction (see also `handleCategoryEdit`, `buildReferralMatch`, `moveContactToCold`).
- **Tests: 162 → 171.** New `setup-spreadsheet.test.js` (9 tests): creates **exactly** the three tabs in order and **none** of the nine legacy lead tabs; the `Leads` header is the 25-column schema with `Details` last and no `Message`/`Asset Class`; `Referrals`/`Subscribers` keep their own schemas; each tab is styled + frozen (header actually written); the **safety guard** — a pre-existing tab **with data and a drifted header** is left completely untouched (no re-header, no styling, no clobber); idempotent second run is all no-ops; throws on missing `SPREADSHEET_ID`; **legacy `setupSpreadsheet` still creates all 11 tabs** (31-col lead tabs, Referral Partners at 32); and the registry **still carries `.tab`/`.tabColor`**. Expected headers are **hand-typed**, not imported from the constants (the write-side mirror of the reader suites' mangled-header rule). Verified by sabotage: removing the empty-tab guard fails exactly the safety and idempotency tests.
- **THE FULL CUTOVER CHECKLIST is now written as `UNIFIED_SCHEMA_MIGRATION_PLAN.md` → §8** — a step-by-step runbook so cutover is executed, not re-derived: `clasp push` → run `setupSpreadsheetUnified()` by hand → flip `USE_UNIFIED_SCHEMA` → `clasp push` + `clasp deploy -i <prod id>` → verify one live submission per lead type → then delete all **13** `xxxLegacy` bodies, the header audit/repair family (10 functions), the EAO backfill family (4), `setCategoryTabStatus`, `reportsEnabledIndex`/`headerIndex`, the legacy column layer (`resolveCols`/`COLS`/`LEAD_HEADERS`/legacy `setupSpreadsheet`), and the registry's `.tab`/`.tabColor`. Includes the rollback (flip the switch back; legacy is intact) and the note that **this is the step where the migration stops being a no-op and ships toward production** — no staging gate, per `CLAUDE.md`.
- **Not deployed, and not live.** The `Leads` tab still does not exist until someone runs `setupSpreadsheetUnified()` by hand at cutover. Nothing to ship for this stage: the switch is off.

## 2026-07-14 (unified-schema migration STAGE 8: the two digests — and the first test coverage they have ever had)

- **refactor(gas): Stage 8 migrates `sendDailyDigest` and `sendMonthlyReferralSummaries`,** the last two readers of the legacy tabs. Same pattern: Unified/Legacy/dispatcher on `USE_UNIFIED_SCHEMA`, still **off** — a **behavioral no-op in production**.
- **Every function that reads or writes lead DATA is now migrated.** Form submission, dedupe, referral matching + stats, the edit trigger, the cold sweep, and now both digests all run against the one `Leads` table under the switch. **What remains is setup/teardown (Stage 9) and the cutover — not lead-data logic.**
- **Building real test coverage was half the job, and is called out as equally important as the port.** Both functions had **zero tests in either schema** before this stage. That is the actual risk: a filter bug (wrong Status excluded, wrong date boundary, wrong `Reports Enabled` check) silently emails the wrong referral partners, or nobody, and nothing catches it. The 12 new tests are written to be a genuine regression net for the **legacy** behavior too, not only a check on the port — the first safety net these functions have ever had. Verified by sabotage: reverting the category filter, or narrowing the `FALSE` check to booleans only, each fails exactly the one test that should.
- **`sendDailyDigestUnified`** reads the one table, keeps the same today's-CT-date filter, and **pulls Asset Class + Booking out of `Details`** — they were top-level columns in the legacy schema but became `Details` keys in Stage 6, so reading them positionally would have silently dropped them from every digest line. (No test would have caught that before this stage, because there were no tests.)
- **`sendMonthlyReferralSummariesUnified`** filters the one table on `Category = 'Referral Partner'` (the tab-membership test becomes a category test) and reads **`Reports Enabled` as a standard resolved column** via `resolveUnifiedCols`. **Answer to the `headerIndex` question in the task:** the older `reportsEnabledIndex`/`headerIndex` pair existed *only* because `Reports Enabled` was a per-tab **extra** outside `LEAD_HEADERS`; under the unified schema it is an ordinary column, so that pair is unused on the unified path and is deleted at cutover. The Referrals-tab tally is **unchanged** — that tab keeps its own schema (`REFERRAL_HEADERS`) and is not part of this migration.
- **Both are read-only, so neither takes the script lock — confirmed, not assumed.** They read rows and send email; they write no cell, so there is no row to race anyone on. A test wires `getScriptLock()` to **throw**, so a future edit that added a lock (or any sheet write) fails a test rather than passing quietly, and asserts the sheets are unmodified.
- **DOC CORRECTION surfaced by writing the first test:** `sendDailyDigest` reads **Lifetime Leads**, not Active Leads. The plan's §3 and `backend-architecture.md`'s trigger table both said Active Leads; the code says Lifetime Leads, and the code is right. Both docs are corrected. This is exactly the drift the standing "verify against the code, never inherit a description" rule exists to catch — and it went uncaught for as long as it did precisely *because this function had no tests.*
- **Tests: 150 → 162.** `sendDailyDigest`: today included / other days excluded, the **CT timezone boundary** (00:00 CT today in, 23:00 CT yesterday out — using a real `America/Chicago` `formatDate` so it does not depend on the host TZ), an unreadable timestamp skipped, an empty table, and a **legacy** test pinning the Lifetime-Leads read + Asset-Class-as-column behavior. `sendMonthlyReferralSummaries`: enabled partner with ≥1 referral gets a summary with the **right count**, an investor with referrals does **not** (category filter), `Reports Enabled = FALSE` (bool **and** string) skipped while blank/`TRUE` send, `Cold`/`Archive` skipped, zero-referral partner skipped, read-only/no-lock, and a **legacy** test pinning the Referral-Partners-tab + per-tab-extra behavior.
- **Fixtures:** two more distinct hand-typed header scrambles (a seventh and eighth), never built from the constants; sandbox constants lifted with `Array.from` before comparison, per the cross-realm rule.
- **Next: Stage 9** — `setupSpreadsheet` (creates `Leads` + `Referrals` + `Subscribers`; **must be run by hand from the Apps Script editor** — `clasp deploy` does not create tabs, and it is what makes the `Leads` tab exist), the `LEAD_TYPES` registry (`.tab`/`.tabColor` come out), and `resolveCols`/`COLS`/`LEAD_HEADERS`. Then the cutover: flip the switch, then delete every `xxxLegacy` body, the header audit/repair family, the EAO backfill family, `setCategoryTabStatus`, and `reportsEnabledIndex`/`headerIndex`.
- **Not deployed, and not live.** The `Leads` tab still does not exist. Nothing to ship for this stage: the switch is off.

## 2026-07-14 (fix: the unified append is name-projected — reader/writer asymmetry CLOSED)

- **fix(gas): every read AND every write on the unified `Leads` table now resolves columns by name.** Stage 7 flagged the last asymmetry in the migration and left it open; this closes it before real submissions can land on it.
- **The bug.** Readers have always resolved by name (`resolveUnifiedCols`) and shrugged off a drifted header. The writer did not: `persistNewLeadUnified` appended `buildLeadRow`'s canonical array **positionally**, assuming the live header still matched. A human reordering — or merely *inserting* — a column in the live `Leads` header would have left **every reader working perfectly** while the writer silently corrupted every subsequent lead. **The readers' own tolerance is exactly what would have hidden it:** the sheet looks healthy, nothing throws, nothing logs.
- **Proven, not asserted.** Reverting to the old positional append and submitting against a reordered header writes: the **Timestamp under `Email`**, the **Match Type (`none`) under `Category`**, the **company under `Phone`**, the **category under `Details`**, and the **role under `First Name`**. Every lead, silently.
- **The fix:** `persistNewLeadUnified` resolves the live header and projects the canonical row onto the sheet's **real** columns (`projectLeadRowByName`) before appending. A reordered header is now handled exactly as every reader handles it. A header **missing** a required column **throws `headerLookupError` and refuses the write** rather than guessing at a cell — the same contract every reader already has, and the same "refusing to run on a broken tab is the correct outcome" principle. A human's **extra** columns past the 25 are preserved as blanks rather than clipped.
- **This is a safety fix, not a behavior change.** On a canonical header the projection is a **verified exact no-op**: a test rebuilds the row directly and asserts the appended cells match byte-for-byte. All 21 of Stage 7's end-to-end submission tests pass unchanged.
- **`buildLeadRow` is deliberately left positional, and that is still correct.** It *constructs* the canonical layout; knowing where the columns actually **are** is the **append's** job. The boundary belongs at the write, not in the row builder — which is why the plan's original sanction of positional row-building was right about `buildLeadRow` and wrong about the append.
- **Tests: 145 → 150.** New coverage: a **reordered** header receiving every value under its own column (13 fields + the `Details` blob explicitly asserted *not* to land in `Phone`); the **whole submission path** — dedupe, referral match, chain, multi-level stats, and the resubmission's blob read-modify-write — all working against a drifted header, so readers and writer are proven to agree on the same sheet; the **canonical no-op**; an **extra column** preserved; and a **missing column refusing the write** with nothing appended. Verified by sabotage: reverting the fix fails exactly the three tests that should fail, and none of the others.
- **Test-suite note:** `submission-path.test.js`'s header comment previously explained *why* the append fixture had to be canonical (because the writer was positional). That reasoning is now obsolete and the comment says so, with the history kept — a stale comment asserting the opposite of the code is how the next person gets misled.
- **Not deployed, and not live.** `USE_UNIFIED_SCHEMA` is off and the `Leads` tab does not exist; this changes nothing in production today. `Code.gs` is touched, so it ships with the eventual cutover deploy.

## 2026-07-14 (unified-schema migration STAGE 7: the submission path — the unified schema can now handle a REAL submission end to end)

- **refactor(gas): Stage 7 migrates the whole submission path** — `findExistingLead`, `existingReferralCodes`, `matchReferrer`, `handleResubmission`, and `persistNewLead` (the extracted append block), with `handleFormSubmission` orchestrating. `USE_UNIFIED_SCHEMA` is still **off**, so this merge is again a **behavioral no-op in production**.
- **THE UNIFIED PATH CAN NOW HANDLE A COMPLETE REAL SUBMISSION, END TO END, FOR ALL FIVE LEAD TYPES.** Payload in → dedupe → collision-checked referral code → referrer match → 25-column row + `Details` blob → **one** append → referral stats credited up the whole chain → Referrals row → Contacts → visitor + partner emails → JSON response. **No previous stage could claim this:** every one of them read or edited a `Leads` table that nothing ever wrote to. Driven for real, not reasoned about — 4 submissions from 3 people produce **3 rows** (dedupe held), a 2-deep chain, and the origin credited **1 direct / 2 downstream**.
- **Why these five could not be split, confirmed rather than assumed:** `findExistingLead` (dedupe) and `existingReferralCodes` (collision check) both scanned **Lifetime Leads**. Migrating `handleFormSubmission` alone would have left both **silently returning "no match"** — every resubmission creating a **duplicate lead**, and referral codes no longer collision-checked. Two silent data-corruption bugs, and both are now pinned by tests.
- **The switch boundary is NOT where the task described, and that was deliberate.** `handleFormSubmission` was **not** duplicated into Unified/Legacy. Only **one block** of it is schema-dependent (three appends + the category-tab check + the `seedReportsEnabled` seed); the normalizer, booking event, Contacts, both emails and the JSON response are byte-identical under both schemas, and its other schema-dependent calls are already dispatchers from earlier stages. So that block was extracted as **`persistNewLead`** and *it* got the three-part treatment. Duplicating the whole function would have meant **two copies of the booking and email orchestration to hand-sync until cutover** — this project's single most-documented failure mode (see the email-template mirrors in `CLAUDE.md`). Every schema-dependent line is still behind the switch; there is simply no duplicated orchestration.
- **`buildReferralMatch` needed no migration** — it takes a row plus a **column map**, and every key it touches (`LEAD_ID`, `FIRST_NAME`, `LAST_NAME`, `EMAIL`, `REFERRAL_CODE`, `REFERRAL_CHAIN`) exists in **both** `COLS` and `UCOLS`. Both `matchReferrer` branches call the same one. **This is the third time** the plan's "retarget this" list has contained a function with nothing to retarget (see also `handleCategoryEdit` and `moveContactToCold`).
- **`handleResubmission` was the sharp edge, and it is not the "simple append" it looks like.** It appended a note to the Message *cell*; Message is now a `Details` key, so it became a **read-modify-write of the JSON blob on a row it did not create** — parse, append, re-stringify, write back. It therefore takes the shared script lock **before the read**, releases in a `finally`, and logs a `MANUAL REPAIR NEEDED` line carrying the dropped message on refusal (non-fatal: the lead still exists and its ID is still returned, so a returning visitor is never told their form is broken). A malformed blob is preserved under `Details._unparsed` rather than destroyed. **A lost counter increment is a wrong number; a lost blob write is a paragraph the visitor actually typed.**
- **Lock discipline held, per the Stage-5 reentrancy rule:** the lock stays **out** of the paths that call `nextLeadSequence()` (`waitLock` on the same lock) and `updateReferrerStats()` (`tryLock`). The suite's lock stub throws `DEADLOCK` on a reentrant `waitLock`, so a refactor that widens the scope fails a test rather than hanging in production.
- **A REAL ASYMMETRY, surfaced by the tests and worth acting on before real submissions land:** every **reader** resolves columns **by name** and survives a mangled header, but the **writer is positional** — `buildLeadRow` constructs the canonical layout and `persistNewLead` appends it (which the plan explicitly sanctions). So **a human who reorders the live `Leads` header silently corrupts every future append**, even though every reader would tolerate the same drift. Not introduced here (it was equally true of nine tabs), but louder with one table. Closing it means projecting the append by name — **a separate change**, recorded in the plan and `backend-architecture.md`.
- **A missing `Leads` table now fails LOUDLY** rather than accepting a submission and dropping it: `persistNewLeadUnified` throws with an actionable message naming `setupSpreadsheet()`. With one table there is no second copy to fall back on — this is the deliberate opposite of the EAO category-tab bug, where `appendRow` logged quietly and every row was lost.
- **Tests: 124 → 145, all green.** New `submission-path.test.js` (21 tests): **one full end-to-end submission per lead type** (correct columns *and* `Details`); **dedupe** (a second identical submission creates **no** second row, routes to the resubmission path, returns the **original** Lead ID, appends to `Details.message`, and is case-insensitive — plus "fills blanks, never overwrites what we knew"); a resubmission surviving a **corrupt `Details` blob**; **referral-code collision** regenerating; **all four match paths** (code/email/name/none) with the chain, the multi-level stats, the Referrals rows and both referrer notifications; `submit_referral`'s structured `Details.referred` surviving the **full** path; the resubmission **lock** (held across the blob RMW, flushed before release) and the **shared-lock contention proof** (a resubmission arriving mid-sweep is REFUSED, writes nothing partial, still returns the real lead ID, and never falls through to the new-row path); **no lock reentrancy**; a missing table failing loudly; and **three legacy tests** proving production still writes **three rows across three tabs**, still dedupes against Lifetime Leads, and still prepends the prose block.
- **Fixture note, and it caught a real design fact:** this suite's append-target header is **canonical** (hand-typed, verified against the constant), not mangled — because the writer is positional, and a scrambled header on an append target models a sheet that cannot exist. A **separate mangled-header fixture** proves the four **read** paths still resolve by name. The first draft mangled everything and failed for a reason that had nothing to do with the stage; that failure is what surfaced the reader/writer asymmetry above.
- **Next: Stage 8** — `sendDailyDigest` + `sendMonthlyReferralSummaries`, the last two readers of the legacy tabs (both currently **untested**; a filter bug silently emails the wrong people, or nobody). Then Stage 9: `setupSpreadsheet` (**must be run by hand from the Apps Script editor** — `clasp deploy` does not create tabs), the registry, and `resolveCols`/`COLS`/`LEAD_HEADERS`. Then §4's delete-outright list at cutover.
- **Not deployed, and not live.** The `Leads` tab still does not exist. Nothing to ship for this stage: the switch is off.
- **CORRECTION (audited 2026-07-15): the two EAO normalizer cleanups Stage 6 flagged "for Stage 7" were NOT done here.** Stage 7 migrated `handleFormSubmission` via the `persistNewLead` extraction and **never touched `normalizeEaoPayload`**. So (a) the `payload.preferences = [eaoDetailsSummary(...)]` JSON-string hack still exists (harmless — neutralized by `buildLeadDetails`'s filter), and (b) EAO's `Details.message` still duplicates `Details.pressing_issue`. Both remain **open, harmless cleanups**; the Stage 6 entry below is corrected to stop implying they were handled.

## 2026-07-14 (unified-schema migration STAGE 6: `buildLeadRow` — §2a and §2b finally SHIP IN CODE)

- **refactor(gas): Stage 6 migrates `buildLeadRow`.** Same pattern: `buildLeadRowUnified` / `buildLeadRowLegacy` (verbatim) / dispatcher on `USE_UNIFIED_SCHEMA`, still **off**, so this merge is again a **behavioral no-op in production**. It builds the plan's 25-column layout (§1, order verified against the plan, not assumed) plus `JSON.stringify(buildLeadDetails(...))` in the last cell.
- **§2a AND §2b STOPPED BEING DECISIONS AND BECAME BEHAVIOR.** Both were resolved in docs on 2026-07-13 and, until this stage, **existed nowhere in code.** • **§2a: all 13 `qualData` fields now persist.** Legacy writes exactly **one** (`assetClasses` → Asset Class) and silently discards twelve — six render an email sentence and are thrown away, six are read by **nothing at all**. The visitor answers, the browser sends, the backend drops it. That ends here. **`submit_referral` is the biggest beneficiary: 100% of its qualified data is discarded today.** • **§2b: `submit_referral.referred` is a structured `{firstName, lastName, email, phone, notes}` object**, retrievable as discrete values, instead of a newline-joined prose block prepended to the Message column. **The prose builder is deleted, not ported.**
- **The per-role field mapping lives in the REGISTRY** (`LEAD_TYPES.detailsFields` + `detailsFrom`), which this project already calls the single definition site for a lead type — so there is no second list to drift. **It is not re-derived from field names**, which is exactly how an earlier draft of the plan got **four of thirteen** assignments wrong (`awareness`/`fit` are `submit_referral`'s, not the investor's; `profession` is the referral partner's, not the RE pro's). EAO is the one type whose fields sit on the payload's **top level** rather than in `qualData` (`detailsFrom: 'payload'`) — its flow never had a `qualData` step.
- **The `Details` contract, decided once and pinned by a test:** a field the lead type **asks** is always **present** as a key (`''`, or `[]` for a list, when the visitor left it blank); a field it does **not** ask is **absent**. So *"asked and not answered"* is distinguishable from *"never asked"* — which is the whole reason for a queryable blob rather than a paragraph. `message`, `preferences`, and `booking` (incl. `meetLink`) are on every type. The derived `assetClass` label is written only when non-empty: it is derived, not collected, and nothing searches it, so it earns no column.
- **`Reports Enabled` is now seeded by the row builder** from `LEAD_TYPES.seedReportsEnabled`, as an ordinary column. The whole `REPORTS_ENABLED_COL = LEAD_HEADERS.length` bug class dies with the per-tab extra.
- **A normalizer hack is now obsolete — flagged for Stage 7, not fixed here. (Update: Stage 7 did NOT fix it — see the 2026-07-15 audit note in the Stage 7 entry below; still open.)** `normalizeEaoPayload` **overwrites** `payload.preferences` with `[eaoDetailsSummary(payload)]` — a JSON **string**, not opt-ins — purely because the legacy schema gave EAO nowhere else to put its detail fields. Those fields now have real `Details` keys, so `buildLeadDetails` **filters that synthetic entry out by exact value** (never by sniffing the string, so a real preference can never be eaten). Verified against the form source: **`buildEAOPayload` sends no `preferences` at all**, so nothing real is dropped. The normalizer should stop doing this — flagged for Stage 7, but Stage 7 never touched the normalizer, so it remains an open (harmless) cleanup.
- **Tests: 105 → 124, all green.** New `scripts/gas/tests/build-lead-row.test.js` (18 tests), and the coverage is deliberately field-by-field rather than shape-only, because getting the role mapping wrong here would be wrong **for real, going forward**: one test per lead type; **every one of the twelve previously-dropped fields named individually** (`aum`, `experience`, `proRole`, `markets`, `profession`, `referralIntent`, `clients`, `proIntent`, `relationship`, `fit`, `timeline`, `awareness`) so a regression names the field it lost; cross-role negatives (an investor row must not carry `awareness`; an RE-pro row must not carry `profession`); the `Details.referred` round-trip as **discrete values**; the prose block asserted **as a negative** in every cell, so a double-write is caught; the blank-field contract; EAO's eight fields and the filtered preferences hack; and **the legacy branch, still 31 columns, still dropping twelve fields, still prepending prose** — because that is what production does and a migration stage must not quietly change it.
- **Fixture rule, and it mattered most here** (this stage *is* the schema): the expected 25-column list in the test is **hand-typed from the plan**, and every assertion reads the produced row through **that** list — never through `UNIFIED_LEAD_HEADERS` or `UCOLS`. A test that indexes the row with the same constant the code indexed it with would pass happily while every column sat one cell to the left.
- **Harness note:** `assert.deepEqual` on a `vm`-realm array fails on the **prototype**, not the contents — Code.gs's arrays carry a different `Array.prototype`. Comparisons against sandbox arrays now go through `Array.from()`. This silently mattered nowhere before because the existing suites only ever asserted `notDeepEqual` on them.
- **Next: Stage 7, the submission path as ONE stage** (`findExistingLead`, `existingReferralCodes`, `matchReferrer`/`buildReferralMatch`, `handleResubmission`, `handleFormSubmission`). **Confirmed against the dependency graph rather than assumed:** `handleFormSubmission` cannot be migrated alone — it calls `findExistingLead` (dedupe) and `existingReferralCodes` (collision check), both of which scan **Lifetime Leads**, and under the unified schema both would **silently return "no match"**: every resubmission would create a **duplicate lead**, and referral codes would stop being collision-checked. Both failures are silent, so they ship together.
- **Still UNFED, and this is the thing to keep in view:** the new row builder has **no caller** yet. `handleFormSubmission` is the only function that appends a lead row, and it is unmigrated — so the `Leads` table remains permanently empty. **Not deployed, nothing to ship: the switch is off.**

## 2026-07-14 (test integrity: six fixture-drift guards were vacuous — cross-realm `notDeepEqual`)

- **fix(gas): the suite's core rule was being enforced by guards that could not fail.** Investigating whether Stage 6's cross-realm `deepEqual` finding affected the already-merged suites turned up a **worse variant of the same bug, in the opposite assertion** — and it inverts the assumption that `notDeepEqual` is the safe direction.
- **The trap.** `Code.gs` is evaluated in a `vm` context with its own intrinsics, so `LEAD_HEADERS` / `UNIFIED_LEAD_HEADERS` carry that context's `Array.prototype`, not the host's. `assert/strict`'s deep comparisons check prototypes with `===`. Therefore: `deepEqual(hostArray, vmArray)` **throws even when the contents match** (loud, self-correcting), but **`notDeepEqual(hostArray, vmArray)` PASSES even when the contents are IDENTICAL** (silent). The second is the dangerous one: it is exactly the tautology the suite's fixture rule exists to prevent.
- **Six guards were vacuous:** the `assert.notDeepEqual(MANGLED, UNIFIED_LEAD_HEADERS)` fixture-drift checks in `referrer-stats`, `cold-sweep`, `status-edit`, `on-sheet-edit`, and `manual-referral-link`, plus `pure-functions`' 5-element literal. Each is meant to prove the hand-typed fixture is not secretly a copy of the constant under test — **and each passed whether it was or not.** Proven by sabotage: a fixture replaced with a byte-for-byte copy of `UNIFIED_LEAD_HEADERS` sailed through the old form and fails the fixed one.
- **The asymmetry that made it invisible:** a fixture built by *transforming* a sandbox constant (`LEAD_HEADERS.slice(5).concat(...)`) stays in the **vm** realm and compares correctly; a fixture *typed by hand* in the test file is a **host** array and does not. Identical-looking assertions, opposite behavior — which is why the rotation-based guards worked and the hand-typed ones did not.
- **Coverage was degraded, not absent.** Every affected file also carries a per-column canonical-index guard (`assert.notEqual(colOf(...), UCOLS[key])`) which compares **numbers**, is realm-agnostic, and was doing the real work — it caught two genuine accidental collisions during Stages 1 and 3. The `notDeepEqual` line beside it was the broken one.
- **Fix:** all 13 header comparisons across the suite now lift the sandbox value with `Array.from(...)` before comparing — uniformly, including the rotation-based ones that happened to work, because they only worked by an accident nobody would remember. The trap is documented at the one place that creates the realm boundary, `tests/helpers/load-code.js`.
- **NO positive `deepEqual` in Stages 1-5 was affected**, and structurally none could be: a cross-realm positive `deepEqual` **fails loudly** rather than passing silently. The only two that compare vm-realm arrays (`leadTabConfigs`, `BOOKING_SLOTS` vs the frontend `SLOTS`) already used `Array.from` with a comment — whoever wrote them had hit this and handled it.
- **Test-only change. `Code.gs` is untouched, so no GAS deploy is required or implied.** 105/105 green.

## 2026-07-14 (unified-schema migration STAGE 5: `handleManualReferralLink` — the edit trigger is now fully wired)

- **refactor(gas): Stage 5 migrates `handleManualReferralLink`,** closing the hole Stage 4 opened. Same pattern: `handleManualReferralLinkUnified` / `handleManualReferralLinkLegacy` (verbatim) / dispatcher on `USE_UNIFIED_SCHEMA`, still **off**, so this merge is again a **behavioral no-op in production**. The referrer lookup moves from a **Lifetime Leads** scan to a `leadsTable()` scan via `resolveUnifiedCols`; **the Stage-4 refusal branch in `onSheetEditUnified` is deleted and the handler is wired in for real.** All three edit routes (Status, Category, Referred By Email) are now live under the unified schema.
- **A hand-linked referral credits the full chain, exactly like an auto-matched one.** The chain is built identically to `buildReferralMatch` (the referrer's own chain plus the referrer's Lead ID — ancestors only, never the new lead), so multi-level `Total Downstream` works through the manual path too: linking Nate to Rita credits Rita **+1 direct, +1 downstream** and her ancestor Olive **+1 downstream only**.
- **THE LOCK-SCOPING TRAP, and why the lock is deliberately narrow.** The script lock covers **only** the read-modify-write of the edited row. Everything downstream runs **outside** it, and must: **`updateReferrerStats()` takes the script lock itself** (Stage 1), and the Referrals append calls **`nextReferralSequence()`, which calls `waitLock()` on the same lock**. **Apps Script does not document the script lock as reentrant** — widening the lock to wrap the whole function would be a second acquisition of a lock the execution already holds, i.e. a deadlock or spurious refusal that would surface **only in production**. The suite guards it: the lock stub's `waitLock()` **throws** if the lock is already held, so a refactor that widens the scope fails a test instead of shipping. **Every later stage that calls `updateReferrerStats`, `nextLeadSequence`, or `nextReferralSequence` inherits this constraint.**
- **NOT routed through `logReferralEntry`, deliberately.** The Referrals row is written inline. That looks like duplication and is not: `logReferralEntry` derives Status as `matchType === 'name' ? 'pending' : 'linked'`, so routing a manual link through it would silently rewrite the Referrals **Status** column from `'manual'` to `'linked'` on every hand-linked referral. (It also takes a `payload`, which an edit trigger does not have.) The inline row is byte-for-byte what legacy writes.
- **⚠ THIS IS NOT "THE LAST CUTOVER BLOCKER". The unified path is now fully wired and completely UNFED.** It closes the only blocker the migration had created *for itself* (Stage 4's refusal), but **nothing writes to the `Leads` table yet**: `buildLeadRow` and `handleFormSubmission` are unmigrated and are the *only* creators of a lead row, so **every migrated function today reads and writes a table that is permanently empty.** Verified by grep, still unmigrated: `buildLeadRow`, `handleFormSubmission`, `findExistingLead`, `existingReferralCodes`, `matchReferrer`/`buildReferralMatch`, `handleResubmission`, `sendDailyDigest`, `sendMonthlyReferralSummaries`, `setupSpreadsheet`, the `LEAD_TYPES` registry, `resolveCols`/`COLS`/`LEAD_HEADERS`, plus the §4 delete-outright families. **Every step of the §6 cutover checklist still stands.**
- **Tests: 96 → 105, all green.** New `scripts/gas/tests/manual-referral-link.test.js`. The headline is an **end-to-end test through the real `onSheetEdit` dispatcher** — not "the refusal is gone" but "the edit actually completes": all seven referral columns back-filled (email lowercased, chain `origin|referrer`, depth 2, match type `manual`), the referrer credited **+1 direct** and her ancestor **+1 downstream only**, the Referrals row appended with Status **`manual`** (asserted explicitly, so a future switch to `logReferralEntry` would be caught), the referrer notified, and **no row created or destroyed**. Plus: an unknown email links nothing and now *logs* (legacy returned silently); a 1-deep chain; a blank cell doing nothing at all; a mangled header refusing to write into a guessed column while releasing the lock; the **shared-lock contention proof** (a manual link driven from *inside* `moveColdLeads`' critical section is REFUSED and writes nothing partial); the **lock-scope proof** (the downstream calls happen after release, and a reentrant `waitLock` would throw); and the **legacy branch**, still scanning Lifetime Leads and writing the same row.
- **Fifth distinct hand-typed header scramble.** Stage 4's now-obsolete "refusal" test was rewritten into its opposite: the route is live and links.
- **Next: Stage 6, `buildLeadRow`** — the 25-column layout + the `Details` blob, where the two data-fidelity decisions (§2a all 13 `qualData` fields; §2b `submit_referral.referred` as structured JSON) actually land. It is pure and self-contained, and everything else in the writer path depends on its `Details` format.
- **Not deployed, and not live.** The `Leads` tab still does not exist. Nothing to ship for this stage: the switch is off.

## 2026-07-14 (unified-schema migration STAGE 4: `onSheetEdit` — and two places the plan is wrong about the dependency graph)

- **refactor(gas): Stage 4 migrates `onSheetEdit`,** the sole dispatcher for all three edit handlers. Same pattern: `onSheetEditUnified` / `onSheetEditLegacy` (verbatim) / dispatcher on `USE_UNIFIED_SCHEMA`, still **off**, so this merge is again a **behavioral no-op in production**. The nine-tab membership guard (`leadTabConfigs()`) collapses to one string compare against `Leads`. The three watched columns still resolve **by name** (now `resolveUnifiedCols`), deliberately unchanged: the dispatch must know *which* column changed, so a drifted header must never be able to route a Status edit into the referral handler.
- **This stage makes `handleStatusEditUnified` reachable.** Until now Stage 3's code could not be exercised at all, because its only caller still guarded on the legacy tabs.
- **The edit trigger is only 2/3 wired, and that is stated rather than glossed.** **Status** → `handleStatusEdit` ✅ migrated, fully wired. **Category** → `handleCategoryEdit` ✅ works **unchanged**. **Referred By Email** → `handleManualReferralLink` ❌ **not migrated, and deliberately NOT called.**
- **FINDING 1 — `handleCategoryEdit` needs no migration at all, and the plan is wrong to say it does.** §3 files it under *"retarget from a tab to the table"*. It has nothing to retarget: it **reads no tab**. Its inputs are `rowData` + a column map, its only column is `EMAIL` (a key present in **both** `COLS` and `UCOLS`), and its body is pure `ContactsApp`. It is already schema-agnostic. Verified by driving it through `onSheetEditUnified` against a unified `Leads` sheet. **Do not write a `handleCategoryEditUnified`.** The same is true of `moveContactToCold`, which takes an email and touches only Contacts. The plan's §3 row has been corrected.
- **FINDING 2 — `handleManualReferralLink` would fail SILENTLY, so `onSheetEditUnified` refuses to call it.** It scans **Lifetime Leads**, which does not exist under the unified schema, and its own missing-tab guard (`if (!lifetimeSheet …) return;`) then returns **quietly**. Wired up as-is it would be a handler that *looks* connected and does nothing: a human types a referrer's email, the edit is accepted, and no referral is linked, no stats updated, no notification sent, and **no error anywhere**. Silently swallowing a human's edit is the exact failure class this migration exists to end, so the unified dispatcher **refuses it and logs loudly** — naming the row, the value, and every consequence that did not happen. This is honest, but it is **a live hole in the unified path**, which makes it a **cutover blocker**.
- **Stage 5 is therefore `handleManualReferralLink`, taken as a DEPENDENCY, not by risk rank** (§3 would have said `sendMonthlyReferralSummaries` next). **This is the second time the real dependency graph has overruled §3's order** — Stage 4 itself was the first. The lesson, now written into the plan: **§3 ranks by blast radius if wrong, which is the right way to rank *risk* but not to rank *order*. Order is set by what is unreachable or broken until X lands.** Use §3 for the first question and the dependency graph for the second.
- **Tests: 87 → 96, all green.** New `scripts/gas/tests/on-sheet-edit.test.js`: the guard (a legacy tab, `Subscribers`, and `Referrals` all ignored — the latter two matter because `resolveUnifiedCols` would *throw* on their schemas, so the guard must reject them before it runs; header row and unwatched columns ignored with **nothing logged**); the routing (a Status edit reaches `handleStatusEdit` and **not** the referral handler; a Category edit reaches `handleCategoryEdit`, clearing the other groups and applying the new one; **routing is by NAME**, proven with a fixture where Status and Category both sit away from their canonical indexes, so a positional dispatcher would misroute both); the **loud refusal** of `Referred By Email` (asserting no referral column is back-filled and the log names the dropped value, the consequences, and Stage 5); a mangled header refusing to dispatch at all; and the **legacy branch**, asserting it still dispatches on the nine-tab guard, still relocates the row, and **ignores the `Leads` table** — so the two schemas can never both dispatch on one edit.
- **Fixture is a fourth distinct hand-typed scramble.** Four suites, four manglings.
- **Not deployed, and not live.** The `Leads` tab still does not exist. Touches `Code.gs`, so a deploy will eventually be needed — nothing to ship for this stage, because the switch is off.

## 2026-07-14 (unified-schema migration STAGE 3: `handleStatusEdit` — and the Stage-2 concurrency prediction was wrong)

- **refactor(gas): Stage 3 migrates `handleStatusEdit`,** the second of the file's two row-deleting functions. Same pattern: `handleStatusEditUnified` / `handleStatusEditLegacy` (verbatim) / dispatcher on `USE_UNIFIED_SCHEMA`, still **off**, so this merge is again a **behavioral no-op in production**. **With this stage, no unified path in the file deletes a lead row anywhere** — both deleters are migrated, and in both the deletion logic was *removed*, not ported.
- **The change: legacy treats a status as a PLACE, unified treats it as a VALUE.** Legacy's `'Cold'` means "copy this row to the Cold Leads tab and delete it from Active"; `'Client'` means "copy it to Clients"; `'Archive'` means "copy and delete". Unified does none of that, because **the human already wrote the cell** — that write is what fired the `onEdit` trigger. The row moves nowhere, nothing is appended, nothing is deleted, and all that remains is the Google Contacts side effect. Most of the function disappears, exactly as the plan said it would. The unified branch **deliberately ignores the `sheetName`/`editedCols` it is handed**, because those come from the not-yet-migrated `onSheetEdit` and are `LEAD_HEADERS`-shaped; feeding a legacy column map to a unified reader is the exact bug class this migration exists to end.
- **CORRECTION — Stage 2's stated fix for the sweep-vs-human-edit gap was wrong, and the correction is the important part of this stage.** Stage 2 predicted the gap would close once `handleStatusEdit` took the same lock. **A lock could never have closed it.** The human's `Status` write is performed by the **Sheets UI**, which takes no lock and cannot be made to; by the time the `onEdit` trigger runs, the cell is *already written*. Giving the handler a lock lets it **notice** a clobber, not **prevent** one.
- **What actually closed it: two mechanisms, both required.** (1) **The shared lock** — `updateReferrerStats`, `moveColdLeads`, and `handleStatusEdit` now all take the one process-wide `getScriptLock()`, so their critical sections genuinely contend and Contacts side effects can never be decided from a row a sweep is halfway through rewriting. (2) **A pre-write live re-read inside `sweepStaleLeadsToCold`** — the sweep re-reads each row's `Status` immediately before stamping `'Cold'` and backs off if it is no longer active, shrinking the clobber window from *the entire sweep* to the microseconds between that read and the write. **This is the part no lock could do**, and it is a (scoped-in, deliberate) touch to Stage 2's function. **The rule to carry forward: a lock only protects against writers that take it.** A race whose other party is a human in the Sheets UI must be handled by re-validating at the point of write.
- **On a status conflict, the handler acts on the LIVE value and does NOT auto-restore.** Contacts side effects are driven off the status read under the lock, never off the (possibly stale) edit event, so the Sheet and Google Contacts can never disagree. The conflict is logged loudly, and the cell is deliberately left alone: nothing can distinguish "a sweep stamped Cold over their Client" from "the human edited again a second later", so auto-restoring would silently revert a deliberate human edit — trading one rare bug for another rather than fixing one.
- **Tests: 75 → 87, all green.** New `scripts/gas/tests/status-edit.test.js`. Its centerpiece is the **shared-lock contention proof**, which is what this stage is actually for: a real stateful exclusive lock (not a stub that always says yes), with `getScriptLock()` returning **the same object** every call as GAS does, and the two functions driven **from inside each other's critical section** — a status edit arriving mid-sweep is REFUSED, a sweep arriving mid-edit is REFUSED, and running them sequentially never contends (so no path leaks the lock). Two independent locks would sail straight through and fail these. Plus: the **clobber guard** (mutating the cell between the sweep's snapshot and its write, proving the human's `Client` survives *and* that the genuinely-stale lead beside it still goes Cold); every unified status path asserting **zero appends, zero deletions**; the live-vs-event conflict; a refused lock skipping only Contacts; and **the legacy branch across all four status paths**, asserting it still appends-and-deletes exactly as production does and takes **no** lock.
- **Honest limits, same as Stages 1 and 2:** none of this proves Apps Script's `LockService` delivers real mutual exclusion — there is no concurrency in a Node harness. What is proven is what is ours to get wrong: that both functions reach for the **same** lock object, that a held lock refuses the other, that neither leaks it, and that the sweep re-verifies before it writes. Real mutual exclusion is Google's guarantee, observable only live.
- **Harness fix:** `FakeRange` had no `getValue()` — a real gap in the fake's fidelity (Apps Script ranges have it), exposed the moment the sweep's re-read became the first code to call it. Added.
- **Next: Stage 4, `onSheetEdit`** — deliberately taken **out of §3's table order** (which would put `sendMonthlyReferralSummaries` next). `onSheetEdit` is the **sole caller** of all three edit handlers, and until it is migrated `handleStatusEditUnified` is unreachable; migrating a caller alongside the handler it feeds is lower-risk than leaving them split across four stages. Also recorded: **`setCategoryTabStatus` is not a stage at all** — it is a §4 delete-outright and cannot go until the cutover removes `moveColdLeadsLegacy`, which still calls it.
- **Not deployed, and not live.** The `Leads` tab still does not exist. Touches `Code.gs`, so a deploy will eventually be needed — but there is deliberately nothing to ship for this stage, because the switch is off.

## 2026-07-14 (unified-schema migration STAGE 2: `moveColdLeads` — row deletion removed, not ported)

- **refactor(gas): Stage 2 migrates `moveColdLeads`,** the next function in the plan's risk-ordered §3 list (🔴 High) and **one of only two functions in the file that delete rows**. Same staging pattern as Stage 1: `moveColdLeadsUnified` / `moveColdLeadsLegacy` / a dispatcher on `USE_UNIFIED_SCHEMA`, which is still **off** — so this merge is again a **behavioral no-op in production**.
- **The change, in one line: a cold lead stops being a *relocated row* and becomes a *status*.** Legacy appends the row to Cold Leads, `deleteRow()`s it out of Active Leads, and re-syncs the duplicate on the category tab — three writes across three tabs to express one status change. Unified sets one cell (`Status = 'Cold'`) and touches nothing else, because with one row per lead there is nowhere to move it to. **The row-deletion path is deleted, not ported** — the plan's largest single safety win. The critical section is extracted as `sweepStaleLeadsToCold`; the loop now runs forward, since it only ran backward to survive `deleteRow`'s reindexing. `setCategoryTabStatus` is **not** called by the unified path (there is no duplicate to sync) but is **not deleted** either — `moveColdLeadsLegacy` still calls it, and it dies at cutover with the rest of the legacy bodies.
- **Script-locked, for a concrete reason:** the sweep has **two entry points** — the Monday trigger *and* the "Run Cold Lead Sweep Now" menu item — so two sweeps can genuinely be in flight at once. They would read the same snapshot, sweep the same leads, and send **two summary emails** claiming the same leads went cold. `moveColdLeadsUnified` takes `tryLock(COLD_SWEEP_LOCK_MS)` (30s) around the read-decide-write, **read included**, flushes before releasing, and releases in a `finally`. A refused lock is non-fatal and loud: it skips the run entirely, does **no** partial work, sends no email, and logs why skipping is safe (the next scheduled sweep picks up the same leads, only staler). The **Contacts calls and the summary email are deliberately outside the lock** — the GAS script lock is process-wide, and holding it across a Contacts round-trip would stall every submission's referral credit.
- **Documented gap, not a silent one — and it is what Stage 3 exists to close:** the lock serializes **sweep vs sweep**, but **not sweep vs a human's `Status` edit**, because `handleStatusEdit` does not take the lock (it is not migrated yet). A human promoting a lead to `Client` mid-sweep can still be clobbered by a stale `Cold` write. Recorded in `backend-architecture.md` → *Known open defects* §5 and in the plan's Stage-3 notes. **A lock only excludes writers that take it**, and claiming otherwise would be worse than no lock.
- **Tests: 64 → 75, all green.** New `scripts/gas/tests/cold-sweep.test.js`: stale active leads go Cold **in place** (asserting **zero deletions, zero appends, unchanged row count, and no lead lost, duplicated, or reordered** — the assertions the whole stage exists for); an ancient **Client** and an already-`Cold` lead are never swept; the age threshold; an unreadable Timestamp is skipped rather than treated as infinitely old; the summary email names exactly the swept leads and no others; a header missing `Status` sweeps nothing rather than stamping `Cold` into a guessed column; the lock (taken **before the read**, `tryLock` never `waitLock`, flushed before release, released on the throwing path, refused-lock does nothing and says so); and **the legacy branch**, asserting it still appends-deletes-syncs *and takes no lock*, so production is byte-for-byte unchanged. The header fixture is hand-typed and mangled **differently from Stage 1's**, on purpose: two fixtures that scramble identically are one fixture.
- **The lock tests do not claim to prove mutual exclusion.** There is no concurrency in the Node harness and a stubbed lock is not a lock; what is provable there is *where the lock sits*, and that is what is pinned. Actual mutual exclusion is Google's guarantee and is only observable live. Same framing as Stage 1, deliberately.
- **Next: Stage 3, `handleStatusEdit`** — the other row-deleting function. It must take the same script lock.
- **Not deployed, and not live.** The `Leads` tab still does not exist; `setupSpreadsheet()` still creates the eleven legacy tabs. Creating it is part of the **cutover**. Touches `Code.gs`, so a deploy will eventually be needed — but there is deliberately nothing to ship for this stage, because the switch is off.

## 2026-07-14 (unified-schema migration STAGE 1: `updateReferrerStats` + multi-level Total Downstream)

- **refactor(gas): the unified-schema migration begins executing. Stage 1 of N migrates `updateReferrerStats`,** the plan's own highest-risk function — it exists in its current form *only* because a lead is duplicated across up to nine tabs, and it is where multi-level attribution has to land (§2c).
- **New schema layer, additive:** `CONFIG.TABS.LEADS` (`'Leads'`), `UNIFIED_LEAD_HEADERS` (the plan's 25 columns, verbatim order), `UCOLS`, `resolveUnifiedCols(sheet)` (same contract as `resolveCols`: resolve by name, **throw** on a miss, never a silent `-1`), `leadsTable()`, and `chainAncestors(chain)`. Nothing but `updateReferrerStats` reads any of it yet.
- **`Total Downstream` is implemented** (ends the defect recorded 2026-07-12): every ancestor Lead ID in the new lead's `Referral Chain` is credited **+1**, walking the whole chain, not just the last link. `Direct Referrals` and `Last Referral Date` still credit the **immediate referrer only** — a lead referred two levels below you is not *your* referral. Both call sites (`handleFormSubmission`, `handleManualReferralLink`) now pass the chain; **the manual-link path credits the full chain too.** The nine-tab loop, the per-tab `resolveCols`/`try`, and the break-after-first-match are gone from the unified path: it indexes the one table by Lead ID once, then does N single-row writes for a chain of depth N.
- **STAGING PATTERN — the load-bearing decision, and Stage 2 must follow it exactly.** `var USE_UNIFIED_SCHEMA = false;` is a **single module-level switch**, and it is **off**. Each migrated function becomes a dispatcher over `xxxUnified` (new) and `xxxLegacy` (the old body, verbatim, marked delete-at-cutover). **With the switch off, this merge is a behavioral no-op in production** — the legacy body runs, byte-identical to before. That is what makes an N-stage migration mergeable one stage at a time without ever leaving `main` half-broken, while honoring the plan's rule that the two schemas must never both be live. Deliberately **not** "does the `Leads` tab exist?" detection, which would flip the whole backend as a side effect of somebody running `setupSpreadsheet()` mid-migration. At cutover: flip the switch, delete every `xxxLegacy`, delete the switch.
- **Tests: 50 → 64, all green.** Three of them cover the lock: that it is taken **before the read** (the assertion that matters — the one a future "tidy-up" would break), that `tryLock` is used and never `waitLock`, that writes are flushed before release, that the lock is released even on the throwing path, and that a refused lock writes nothing and logs a repairable message. **What they deliberately do not claim:** that `LockService` really provides mutual exclusion. There is no concurrency in a Node harness, and a stubbed lock is not a lock — a test "proving" two racing submissions are serialized would be testing the stub. That guarantee is Google's and is verifiable only in the live runtime; what is ours to get wrong is *where the lock sits*, and that is what is pinned. New `scripts/gas/tests/referrer-stats.test.js` also covers a **4-deep** chain (every ancestor +1, asserted as exact values on every row, seeded from non-zero all-different counters so "+1" cannot pass as "= 1"), the **`Direct Referrals` regression** (immediate referrer only; the three ancestors above unchanged), the 1-deep and no-referrer boundaries, a chain repeating an ancestor (credited once — the counter counts leads, not chain entries), a chain naming a lead with no row (the real ancestors are still credited, the broken link is logged), a header miss (throws, never writes to a guessed cell), and **the legacy branch** (proof production is untouched today). Its header fixture is **hand-typed and mangled** away from `UNIFIED_LEAD_HEADERS` per the suite's core rule — and a guard test asserts no column sits at its canonical index, which caught a real accidental coincidence while the fixture was being written.
- **Not deployed, and not live.** The `Leads` tab does not exist in the Sheet; `setupSpreadsheet()` still creates the eleven legacy tabs. Creating it is part of the **cutover**, not of any single stage. **This touches `Code.gs`, so the usual two-step GAS deploy (`clasp push` **then** `clasp deploy -i <prod id>`) will be required eventually — but there is deliberately nothing to deploy for this stage, because the switch is off.**
- **Concurrency: the counter read-modify-write now holds a script lock** (added on review, before merge). One submission writes N ancestor rows, so two submissions on overlapping chains could interleave read-read-write-write and permanently lose an increment — silently, on the number the referral product is measured by. `updateReferrerStatsUnified` now takes `LockService.getScriptLock().tryLock(10s)` **around the whole read-modify-write, the read included** (locking only the writes would leave the race entirely intact, since both executions would already have read the same stale counter), flushes before releasing, and releases in a `finally` so a mangled header cannot leak the lock. **`tryLock`, not `waitLock`:** a blocked execution gives up rather than sitting on a slot until the 6-minute cap. **A refused lock is neither fatal nor silent:** it does not throw (this runs inside `handleFormSubmission`'s try, so throwing would fail the visitor's submission because someone else submitted at the same moment), it applies **no partial credit**, and it logs a `MANUAL REPAIR NEEDED` line carrying the referrer and the full chain so the credit can be replayed by hand.
- **Resolved a contradiction inside the plan itself: there is NO top-level `Message` column in the unified schema.** §1's 25-column list (normative) never had one and `message` is a `Details` key, but §2b's phrasing ("the **Message** column goes back to holding only what the submitter typed") read as if one survived. §2b now says `Details.message`. Message fails both halves of the top-level rule — nothing searches it across rows, and `onSheetEdit` does not watch it — so the blob is where it belongs; that it is *important*, or nice to read in the grid, are explicitly not reasons. **Consequence flagged for a later stage:** `handleResubmission` appends its resubmission note to the Message **cell** today, and must become a read-modify-write of `Details.message` rather than a write to a column that no longer exists. `updateReferrerStatsUnified` was checked and touches Message nowhere (`UCOLS` has no `MESSAGE` key at all).

## 2026-07-13 (migration plan is DECISION-COMPLETE — all 3 decisions resolved)

- **decision(infra): the final two open decisions are resolved. `UNIFIED_SCHEMA_MIGRATION_PLAN.md` is now decision-complete and ready for execution** — nothing in it is waiting on an answer.
- **§2a RESOLVED — the 12 under-persisted `qualData` fields get FIXED by the migration, not ported as-is.** Every `qualData` field a lead type collects is written into that type's `Details` JSON blob. This ends a **pre-existing silent data-loss bug** (the visitor answers, the browser sends, the backend discards) rather than faithfully re-implementing it in code being written from scratch. Emails are unaffected — persisting a field does not change what the email says. **`submit_referral` is the biggest beneficiary: all three of its qualData fields are in the never-read-at-all group, so 100% of that lead type's qualified data is discarded today.**
- **§2b RESOLVED — `submit_referral`'s referred-person data becomes structured JSON** under `Details.referred` (`{firstName, lastName, email, phone, notes}`), replacing the unparseable prose block `buildLeadRow` currently prepends to the **Message** column (`Code.gs:1715-1728`, deleted not ported). **Deliberately NOT a top-level column:** per the plan's established rule, a field earns a real column only if code must *search* it across rows or `onSheetEdit` must detect an edit to it. This satisfies neither — `matchReferrer` searches for a *referrer* against that referrer's own lead row and never scans other leads' submissions for a referred-person block, and the data is scoped to the single submitting row. Promoting it would add five always-blank columns to every other lead type, reintroducing the exact problem the migration deletes.
- **corrected while verifying: the plan's per-lead-type `qualData` mapping was wrong on 4 of 13 fields.** The original draft inferred it from field *names*; re-verified against the form source (`Step2Context.tsx` role gates, `Step3AssetClass.tsx`, `ContactForm.tsx:159`), the real mapping is **investor** → `aum`/`experience`/`assetClasses`/`timeline` (the last two from the `prefs` step, which **only** the investor flow reaches), **referral** → `profession`/`clients`/`referralIntent`, **pro** → `proRole`/`markets`/`proIntent`, **submit_referral** → `relationship`/`fit`/`awareness`. `awareness` and `fit` had been filed under Investor, `profession` under RE Professional, and `relationship` under Referral Partner. The plan now carries the verified table and an explicit warning not to re-derive it from field names.
- **test strategy extended:** the previously-dropped `qualData` fields must be proven to land in `Details` on **at least two lead types** (Investor and submit_referral recommended), asserted field-by-field against the **parsed** blob; and `submit_referral`'s `referred` block must be proven to **round-trip as retrievable structured JSON**, with the negative asserted too (the `Referred person:` prose no longer appears in Message, so a double-write regression is caught).
- **docs only — no `Code.gs` change, so no GAS deploy is required or implied.**

## 2026-07-13 (migration decision resolved: Total Downstream gets built)

- **decision(infra): `Total Downstream` is RESOLVED — implement it as real multi-level referral attribution, do not retire it.** Credit propagates up the **entire** chain, not just to the immediate referrer: if John refers Steven and Steven later refers Maria, Maria counts toward **both** Steven's and John's Total Downstream. `Direct Referrals` is unchanged and still increments for the immediate referrer only; Total Downstream is a new multi-level count layered alongside it. **This is a computation fix, not a data-collection gap** — `Referral Chain` already stores exactly the ancestor list needed (verified `Code.gs:1582-1601`: a new lead's chain is `referrerChain + '|' + referrerLeadId`, so it holds every ancestor, origin-first, and **not** the new lead's own ID). The fix is to walk that chain and increment every ancestor. It lands **inside the `updateReferrerStats` rewrite** (already the highest-risk item), because multi-level counting writes to many ancestor rows at once and that is only safe once the unified schema ends row duplication across nine tabs. Gets dedicated test coverage: a ≥3-level chain asserting every ancestor increments, plus a regression check that Direct Referrals still credits the immediate referrer only. Two decisions remain open (the 12 under-persisted `qualData` fields; `submit_referral`'s prose-in-Message block).
- **Consequence:** the target schema grows to **25 columns** — `Total Downstream` must be a real top-level column, not a `Details` key, because it is a counter read-modify-written on rows *other than* the one being inserted. The 2026-07-12 entry below says 24; it was accurate when written.
- **docs only — no `Code.gs` change, so no GAS deploy is required or implied.**

## 2026-07-12 (unified-schema migration plan + a docs correction pass)

- **docs(infra): created `/docs/UNIFIED_SCHEMA_MIGRATION_PLAN.md`** — an actionable plan (not notes) for collapsing the nine lead tabs into one `Leads` table: the target schema (24 real columns + a `Details` JSON blob, generalizing the `eaoDetailsSummary` → Preferences pattern already in production), the three **open decisions** left deliberately undecided (the 12 under-persisted `qualData` fields, `submit_referral`'s prose-in-Message referred block, `Total Downstream`), a function-by-function rewrite list with `updateReferrerStats` flagged highest-risk (it exists *only* because of row duplication across 9 tabs and collapses to a single-row lookup), the deletion list (the entire header audit/repair family and the EAO backfill family become obsolete), a test strategy that lands coverage **with** the rewrite, and a rollout sequence. Scope is the Sheet schema only — **no `apps/api`, no dashboard, no admin actions.**
- **docs: corrected a real contradiction between two docs.** `frontend-payload-schemas.md` claimed `heardAbout` was "transmitted and dropped — there is no `Heard About` column in `LEAD_HEADERS`", while `backend-architecture.md` correctly documented it as **column 31**. The backend doc was right; the frontend doc was describing the pre-2026-07-08 state. Verified against `Code.gs:1761` (`buildLeadRow` writes `leadHeardAbout(payload)`). The two files now agree.
- **docs: recorded three pre-existing gaps that were true but undocumented.** (1) **12 of 13 `qualData` fields never reach the Sheet** — `buildLeadRow` persists only `assetClasses`; six fields (`aum`, `experience`, `proRole`, `markets`, `profession`, `referralIntent`) render an email sentence and are discarded, and six (`clients`, `proIntent`, `relationship`, `fit`, `timeline`, `awareness`) are read by **nothing at all**. Pre-existing, not a migration artifact. (2) **`Total Downstream` (col 28) is written by nothing** — seeded `0`, and `updateReferrerStats` only ever touches `Direct Referrals`/`Last Referral Date`, so it is permanently zero on every row. (3) **`createContact` fails silently** for at least one confirmed case; `ContactsApp` (the legacy Contacts API) is the leading suspect but is **not** a confirmed diagnosis. Future work must not assume Contacts creation works.
- **audit: the four "callable admin actions" do not exist and never did.** A repo-wide grep for `setLeadStatus`, `setReportsEnabled`, `forcePartnerSummaryNow`, `forceDailyDigestNow` returns **zero matches** — in `Code.gs`, in the frontend, and in `/docs`. No document ever claimed they were built, so nothing needed retracting; `backend-architecture.md` now states their absence explicitly so no future session assumes otherwise. `doPost` routes to exactly two handlers and `doGet` to two; the only manual levers are the **AxisPoint** Sheet menu and the Apps Script editor.
- **audit: `Curious` is an orphaned tab** — referenced by no code (not in `CONFIG.TABS`, not in `LEAD_TYPES`, not created by `setupSpreadsheet`, never read or written). A leftover of the pre-EAO role vocabulary. Safe to delete from the live Sheet; left alone here only because this repo cannot read the Sheet to confirm its contents first.
- **audit: no code changes needed.** `pnpm test:gas` passes **50/50**; CI (`CI` + `GAS tests`) is green on `main`. The two `Deploy` workflows fail with `Input required and not supplied: server` — the **known, pre-existing** missing FTP secrets, not a regression. The two stray `eao-email-preview*.html` files flagged earlier are **already gone** (removed 2026-07-10). Working tree clean, no untracked strays. All five OAuth scopes in `appsscript.json` are exactly justified by real usage (`GmailApp.sendEmail` → `gmail.send`; `SpreadsheetApp.openById` → `spreadsheets`; `ContactsApp` → `contacts`; `Calendar`/`CalendarApp` → `calendar`; `ScriptApp.newTrigger` → `script.scriptapp`) — **none unused, none missing.** Code.gs carries no `TODO`/`FIXME` comments.
- **docs only — no `Code.gs` change, so no GAS deploy is required or implied by this entry.**

## 2026-07-11 (`.claspignore` — the test suite must never reach Apps Script)

- **chore(gas): added `scripts/gas/.claspignore` as an allowlist** (`appsscript.json`, `Code.gs`, `emails/**` only). Caught while deploying the 2026-07-10 work: `.clasp.json` sets `skipSubdirectories: false` with `.js` in `scriptExtensions` and no ignore file existed, so `clasp status` listed all 7 files of the new `scripts/gas/tests/` suite as **tracked** — `clasp push` would have uploaded them. Apps Script executes every pushed file's top-level statements in one shared global scope per invocation, and the tests open with `require('node:test')`, so the push would have thrown `ReferenceError: require is not defined` on every `doPost` and every trigger: a full backend outage sourced entirely from non-`Code.gs` files. Written as an allowlist so future Node tooling under `scripts/gas/` is excluded by default rather than by someone remembering. See `/docs/deployment.md`.
- **Deployed:** production deployment `AKfycbzf…IXeZgBqg` redeployed at **@23**, first version carrying `resolveCols`.

## 2026-07-10 (committed GAS test suite + resolveCols kills the positional-read bug class)

- **test(gas): first committed test suite** at `scripts/gas/tests/`, run with `pnpm test:gas` and in CI via the additive `.github/workflows/test-gas.yml` (Node 22; `ci.yml`/`deploy-web.yml`/`deploy-qr.yml` untouched). `vm`-loads the real `Code.gs` with GAS globals stubbed. Covers the 25 previously-untested pure functions, the seven embedded `TEMPLATE_*` ↔ `emails/*.html` parity pairs, and the `BOOKING_SLOTS`↔frontend `SLOTS` sync. **Header fixtures are deliberately mangled away from `LEAD_HEADERS`** (reordered/re-cased/whitespace/rotated), the explicit lesson from the tautological harness that missed the 2026-07-08 bug.
- **feat(gas): `resolveCols(sheet)` — the new standard for reading a live lead tab's columns.** Reads the tab's actual header row and returns a `COLS`-shaped object of real positions matched by name (`findHeaderIndex`); **throws `headerLookupError` on a genuine miss, never a silent `-1`**. Resolves the 31 standard columns only (Referral Partners' `Reports Enabled` stays with `reportsEnabledIndex`; Referrals/Subscribers keep their own schemas).
- **refactor(gas): all 14 live-sheet readers threaded through `resolveCols`** — `existingReferralCodes`, `findExistingLead`, `matchReferrer`/`buildReferralMatch`, `updateReferrerStats`, `handleResubmission`, `sendDailyDigest`, `sendMonthlyReferralSummaries`, `moveColdLeads`, `setCategoryTabStatus`, `onSheetEdit`, `handleManualReferralLink`, `handleStatusEdit`, `handleCategoryEdit`. `moveColdLeads` (the only reader that also deletes rows) was converted and verified in isolation first, with a drifted-header integration test; dedupe and edit-sync got the same. This closes the positional-read bug class documented in the 2026-07-09 audit: a drifted header now reads the right cell or fails loud, instead of silently corrupting data. Fresh-row builders (`buildLeadRow`) still use `COLS` positionally, by design.
- **fix(gas): two audit-found bugs fixed in the same pass.** `updateReferrerStats` now derives its tab list from `leadTabConfigs()` (the old hardcoded 7-tab list omitted Clients and Archive, so a referrer promoted to Client stopped accruing referral stats). `createContact` routes Source through `leadSource()` instead of `payload.source || payload.page`, matching every other consumer. `onSheetEdit` gained a lead-tab guard so edits to Referrals/Subscribers are no longer dispatched to lead handlers.
- **chore(gas): removed dead/dangerous one-time tooling** — `migrateAddHeardAboutColumn` (positional-placement loaded gun, the tool that corrupted the Lifetime Leads header), `auditLeadTabHeaders` (all-tabs dump the log viewer truncated; superseded by the summary + per-tab detail), and `repairLifetimeLeadsHeader` (one-line alias of `repairLeadTabHeader`), plus two untracked `eao-email-preview*.html` files at the repo root.
- **docs:** corrected the "Sheet holds real production data" premise in the per-tab-vs-unified schema decision — Lifetime Leads is confirmed **empty** (visually 2026-07-10; `rows=0` in the 2026-07-09 audit), so that justification was false; the automation-coupling reason still stands. Documented `resolveCols` as the mandatory standard for live-sheet column reads.
- **still requires a manual GAS deploy** (`clasp push` then `clasp deploy -i <prod id>`) before any of this is live. Merging alone does not ship it.

## 2026-07-09 (bulk header repair across every drifted lead tab)

- **context:** `auditLeadTabHeadersSummary()` was run live and answered the question the previous entry left open. **Eight of the nine lead tabs are drifted, all with `rows=0`, all reporting `rewrite=SAFE`:** Active Leads, Lifetime Leads, Cold Leads, Investors, Referral Partners, RE Professionals, Clients, Archive. Only `Existing Asset Owners` is `OK`. Repairing them one at a time through a Lifetime-Leads-specific function was the wrong shape.
- **feat(gas): `repairAllDriftedLeadTabHeaders()`.** Iterates `leadTabConfigs()`, and for each tab the audit reports `rewrite=SAFE` clears and rewrites row 1 from `expectedHeadersFor()` — 32 columns on Referral Partners, 31 everywhere else. **The safety gate is `leadTabHeaderAudit().safeToRewrite` used verbatim,** not a second implementation of "is this safe": drifted-and-empty rewrites, everything else is skipped and logged. Per-tab result is one of `REPAIRED`, `SKIPPED (already OK)`, `SKIPPED (unsafe: has data, needs manual review)`, `SKIPPED (tab not found)`, followed by a tally. Idempotent.
- **refactor(gas): `repairLifetimeLeadsHeader()` generalized, not duplicated.** Its body is now `rewriteLeadTabHeaderRow(sheet, tabName)` (mechanical rewrite + the zero-data-row assert) and `repairLeadTabHeader(tabName)` (audit-driven single-tab repair, works for any lead tab). `repairLifetimeLeadsHeader()` survives as a one-line alias. The data-row assert lives in exactly one function, beneath both the single and bulk paths, so no caller can route around it.
- **behavior change:** a *healthy* tab is now left completely untouched rather than idempotently rewritten. `repairLifetimeLeadsHeader()` previously re-wrote an already-correct empty header; it now reports "already matches" and writes nothing. Same resulting sheet, no needless write.
- **fixed while here:** a tab whose old header was *wider* than expected (e.g. a stray 32nd column on a 31-column tab) kept the bold coloured fill on the now-blank trailing cells, which reads as a real column. `rewriteLeadTabHeaderRow()` clears formatting past the expected width.
- **verify(gas):** Node harness (`vm`-loaded real `Code.gs`, fake sheets counting every write). Seeded to the live drift pattern: six drifted+empty standard tabs, a Referral-Partners-shaped tab at 31 columns needing 32, a *wider*-than-expected Archive at 32 needing 31, a healthy `Existing Asset Owners`, and a **drifted `Clients` tab holding a data row**. Confirms: Referral Partners lands on exactly 32 columns with `Heard About` at 31 and `Reports Enabled` at 32; the other six land on exactly `LEAD_HEADERS` (31); **`Clients` and `Existing Asset Owners` receive literally zero writes** (mutation counter, not eyeballed); `repairLeadTabHeader('Clients')` and a direct `rewriteLeadTabHeaderRow()` call both throw *before* mutating; the second bulk run is `0 repaired, 8 already OK, 1 unsafe`; a deleted tab is skipped, not fatal; and `repairLifetimeLeadsHeader()` still repairs Lifetime Leads without touching any other tab.
- **still requires a manual GAS deploy** (`clasp push` then `clasp deploy -i <prod id>`) before the function exists in the Apps Script editor. Merging alone does not ship it.

## 2026-07-09 (condensed header-audit summary mode)

- **chore(gas): `auditLeadTabHeaders()` was unreadable in practice.** It emits a full 31-column header row per tab, and the Apps Script log viewer truncated the message before `Referral Partners`, `RE Professionals`, `Existing Asset Owners`, `Clients` and `Archive` ever rendered — so the five tabs nobody had ever inspected were exactly the five the audit could not show.
- **feat(gas): `auditLeadTabHeadersSummary()`,** one line per tab (`OK`/`DRIFT`, `rows=`, `cols=actual/expected`, `rewrite=SAFE|NO(has data)`, count of missing name-looked-up headers) plus a drifted-tab footer. ~660 chars for nine tabs against ~6,300 for the full audit. Deliberately prints the *count* of missing key headers, not their names: three long header names across nine tabs is the unbounded growth this mode exists to avoid.
- **feat(gas): `auditLeadTabHeaderDetail(tabName)`,** the drill-down the summary points at. Full per-column diff for one tab, which is now the only way to read a drifted tab's columns without the other eight crowding it out of the log. Throws on an unknown tab name.
- **refactor(gas): `leadTabHeaderAudit(ss, tabName)` extracted** as the single analysis pass, with `renderLeadTabHeaderDetail()` as its shared renderer. All three audit entry points read one verdict object, so the summary cannot report `OK` on a tab the detail calls `DRIFT`. `safeToRewrite` is defined as **drifted AND empty**, never merely empty, so it only ever points at tabs that both need a repair and can take one.
- **verify(gas):** Node harness (`vm`-loaded real `Code.gs`). Differential test asserts `auditLeadTabHeaders()` output is **byte-identical** before and after the refactor. Summary asserted at 662 chars / 13 lines (9.5× smaller, longest line 82 chars), containing all nine tabs by name. Semantics covered against a seeded world: a drifted+empty tab reports `rewrite=SAFE`, a **drifted tab holding data** reports `rewrite=NO(has data)`, a healthy+empty tab reports `safeToRewrite === false` rather than being invited to a pointless rewrite, Referral Partners reads `cols=32/32`, a deleted tab reports `MISSING` instead of crashing, and the detail function names the missing `Lead ID`, excludes other tabs, and throws on an unknown or absent tab name.
- **not claimed:** the harness bounds the log size, it does not reproduce Apps Script's exact truncation threshold. The truncation itself is an observed behavior of the live log viewer.

## 2026-07-09 (Lifetime Leads header repair + cross-tab header audit)

- **feat(gas): `auditLeadTabHeaders()`, read-only.** Dumps every lead tab's data-row count and live header row, diffed per column against `expectedHeadersFor()` and flagging any missing name-looked-up header (`Lead ID`, `Category`, `Heard About`). Nothing in the script had ever read a header row purely to check it, so drift was only ever discovered when an unrelated function tripped over it. This is the first direct way to see the live layout of all nine lead tabs.
- **feat(gas): `repairLifetimeLeadsHeader()`, guarded.** Clears row 1 across the sheet's full width (the drifted header has trailing/blank cells a bare `setValues` would leave behind) and rewrites it from `expectedHeadersFor()`. **Throws rather than running when the tab holds data rows.** `leadRow()` builds a positional 31-value array and `appendRow()` writes it to Lifetime Leads on every submission *without reading the header*, so a wrong header does not imply wrong rows — and rewriting a header over rows written under an older layout moves no cells, it silently relabels every column, after which name-based readers like `eaoBackfillPlan()` copy the wrong cells while appearing healthy. The guard makes that class of corruption unreachable.
- **refactor(gas): `expectedHeadersFor(tabName)` extracted** from the inline ternary in `setupSpreadsheet()`, now the single definition site for a healthy lead-tab header row (`LEAD_HEADERS`; `+ Reports Enabled` on Referral Partners only). Three functions now agree by construction about what a healthy tab looks like.
- **corrected a stale premise:** `Heard About` has been the last element of `LEAD_HEADERS` since it was appended there, so the tempting `LEAD_HEADERS.concat(['Heard About'])` rebuild produces a **32-column header with a duplicate `Heard About`**. Lifetime Leads' correct header is exactly `LEAD_HEADERS` (31 columns). Noted at the `REPORTS_ENABLED_HEADER` definition and in `expectedHeadersFor`'s comment.
- **verify(gas):** Node harness (`vm`-loaded real `Code.gs`) reproducing the reported corrupt Lifetime Leads header (legacy 18-column layout + `Date Submitted`, 12 blanks, `Heard About` stranded at col 31 — the shape `migrateAddHeardAboutColumn()` leaves when it writes into col 31 of an 18-column tab). Confirms: repair yields exactly `LEAD_HEADERS`, one `Heard About`, no blanks, no `Date Submitted`, `Lead ID`/`Referral Code` restored at cols 2/3; the naive `+ 'Heard About'` rebuild duplicates the column; the data-row guard throws and leaves the header untouched; `countMissingEaoCategoryRows()` throws `No "Lead ID" header` before the repair and returns a clean count after; and `auditLeadTabHeaders()` marks a healthy 31-col tab `OK` and Referral Partners `OK` at 32.
- **not done, deliberately:** the eight other lead tabs' live headers were **not** read. `clasp` credentials lack the Sheets scope, so no tab's real header row can be read from this repo — `auditLeadTabHeaders()` must be run from the Apps Script editor to answer that. No other tab was auto-fixed.

## 2026-07-09 (resilient header matching in the EAO backfill)

- **fix(gas): `eaoBackfillPlan` matched header names with an exact, case-sensitive compare against a hardcoded literal, and threw `No "Lead ID" header on Lifetime Leads` against the real Sheet.** Expected names are now **derived** from the schema (`LEAD_ID_HEADER = LEAD_HEADERS[COLS.LEAD_ID]`, likewise `CATEGORY_HEADER` and `HEARD_ABOUT_HEADER`) instead of being a fourth hand-typed copy, and all matching goes through `normalizeHeaderName` (strips `U+200B..U+200D` zero-width; collapses every whitespace run, which in JS `\s` already includes `U+00A0` NBSP and `U+FEFF` BOM; trims; lowercases). `Lead ID` / `lead id` / `Lead  ID` / `Lead<NBSP>ID` / `  LEAD ID ` now all match; `LeadID` and `Referred By Lead ID` correctly still do not. `REPORTS_ENABLED_HEADER` stays a literal (a per-tab extra, genuinely absent from `LEAD_HEADERS`).
- **fix(gas): the same exact-compare existed a second time, in the column projection, and was the more dangerous of the two.** `dstHeaders.map(h => srcHeaders.indexOf(h))` would have written **blank** into any column whose two header cells differed only in case or spacing — including `Lead ID` itself. A blank key is invisible to the `seen` set, so a second run would not recognize those rows and would **insert duplicates**. The function's entire idempotency guarantee rested on an exact string match between two independently hand-edited header rows. Both sites now use a prebuilt normalized `srcIndexByHeader` map (first occurrence wins, `hasOwnProperty`-guarded).
- **fix(gas): a failed lookup is now self-diagnosing.** `headerLookupError` logs and throws the full live header row rendered character by character (`col 2: "LeadID"  len=6  codes=[76,101,97,100,73,68]`), plus the expected name and its normalized form, so a genuine mismatch is readable straight from the execution log with no separate probe function.
- **not changed, deliberately:** `headerIndex(sheet, name)` still matches exactly. Its callers are `reportsEnabledIndex` and `migrateAddHeardAboutColumn`, and in the latter a `-1` drives a **write** (it inserts a column), so loosening the match alters migration behavior on live tabs. Flagged for its own change rather than folded into this one.
- **verify(gas):** new Node harness (`vm`-loaded real `Code.gs`), 46 assertions, all passing — and unlike the prior harness it never builds a header row from `LEAD_HEADERS`, which is exactly why the original bug was invisible to testing. Every source header row is deliberately mangled away from the constant. Covers: 14 drift variants all normalizing to `lead id`; four non-collision cases that must stay distinct; the exact live failure reproduced (`Category` matching while `Lead ID` does not) and then resolving; a full backfill across a source and destination whose headers are mangled **differently from each other**, asserting `Lead ID` and `Email` values land in the right destination columns rather than blank; **idempotency re-verified under mangled headers** (run 2 inserts 0, no duplicates), which is the regression the projection fix prevents; and a genuinely-absent header still throwing, with the char-code dump reaching both the `Error` and `Logger`. The two prior harnesses (registry: 46 assertions; differential old-vs-new: 0 regressions) both still pass unchanged, confirming the derived constants are byte-identical to the literals they replaced.
- **still unconfirmed:** the exact live header cell value was never captured (the `clasp` token has no `spreadsheets` scope), so the *specific* drift is inferred, not observed. The fix covers the whole class; if the live cell turns out to be a genuinely different name (e.g. `LeadID`), the new diagnostic will now say so precisely on the next run.

## 2026-07-09 (LEAD_TYPES registry — one definition site per lead type)

- **refactor(gas): added `LEAD_TYPES`, the single definition site for a lead type, and rewired every consumer to derive from it.** Each entry carries `category`, `tab`, `contactGroup`, `tabColor`, `normalizer`, `seedReportsEnabled`. `roleToCategory`, `categoryTabForRole`, `contactGroupForCategory`, `handleFormSubmission`'s normalizer dispatch and Reports-Enabled seed, `handleCategoryEdit`'s group-clear list (via `allCategoryContactGroups()`), and the tab lists in **both** `setupSpreadsheet()` and `migrateAddHeardAboutColumn()` (via `leadTabConfigs()`) now all read the registry instead of each maintaining a parallel hand-kept list. `submit_referral`'s `tab: null` / `contactGroup: null` are documented as assertions, not omissions. `'Client'` stays out of the registry (it is a status-derived Category, not a wire role) and is special-cased in `contactGroupForCategory`. Incidental hardening: `leadTypeFor` guards with `hasOwnProperty`, so a POSTed `role: "constructor"` now resolves to `null` instead of returning the `Object` *function* as a Category (the old plain-object lookup did, and `|| ''` never caught it because the value was truthy).
- **fix(gas): Existing Asset Owner's three real gaps, exposed and closed by the registry.** (1) `CONFIG.CONTACT_GROUPS` had **no EAO entry**, so `contactGroupForCategory('Existing Asset Owner')` returned `null` and every EAO lead was created as a Google Contact belonging to no category group; added `ASSET_OWNERS: 'AxisPoint Existing Asset Owners'`, which also enrolls it in `handleCategoryEdit`'s clear list so re-categorizing a contact no longer leaves the group stuck on. (2) The `Existing Asset Owners` tab was named in `CONFIG.TABS` **and already listed in `setupSpreadsheet`** — it simply never existed, because `setupSpreadsheet` was never re-run after EAO shipped; `appendRow()` logs-and-returns on a missing tab, so **every EAO category-tab row has been silently dropped since launch**. (3) `handleFormSubmission` now checks the tab exists before appending and logs loudly when it does not, rather than leaning on `appendRow`'s quiet log. Lifetime/Active always received the row, so nothing was lost and the data is recoverable.
- **fix(gas): added `backfillEaoCategoryRows()` + read-only `countMissingEaoCategoryRows()`.** Copies the dropped EAO rows out of Lifetime Leads into the newly-created tab. **Idempotent** — keyed on `Lead ID` against the destination, so a second run inserts nothing and a later run picks up only genuinely-new rows. Columns are projected **by header name**, never by position, per the standing name-based-lookup rule, so source and destination may differ in column order or width. Throws an actionable error if the tab does not exist yet rather than no-oping. **Requires `clasp push` + `clasp deploy -i`, then one manual `setupSpreadsheet()` run to create the tab, then one `backfillEaoCategoryRows()` run** — same manual-run pattern `migrateAddHeardAboutColumn()` needed; `clasp deploy` does not create tabs.
- **corrected two premises rather than implementing them.** (a) `bookingEventInternalDescription`'s `payload.role === 'existing_asset_owner' && payload.current_situation` check was reported as reading a raw pre-normalized payload. It does not: `normalizeEaoPayload` mutates in place and *adds* the generic fields without stripping role-specific ones, and the function's single call site is downstream of normalization, so both values are present. Rewriting it to parse `current_situation` back out of the Preferences JSON blob would re-derive a value already on the payload. Left as-is, with a comment saying why. (b) `buildEAOPayload` was reported as needing to send `heardAbout` "like every other builder." `buildPayload` sends `heardAbout: s.sourceSel`, and `sourceSel` is set only by `Step4Contact`, which `STEP_ORDER_EAO` does not include — the EAO flow never asks the question. Since `leadHeardAbout()` already yields `''` for a missing field, adding `heardAbout: ''` would change no cell and only make a hardcoded blank look like a captured answer. Documented as a **product** gap (add the question to the EAO step order) in `utils.ts` and `backend-architecture.md`. **No behavior change shipped for either.**
- **verify(gas):** two Node harnesses (`vm`-loaded real `Code.gs`, stubbed GAS globals). (1) 46 assertions, all passing: all five lead types resolve `category`/`tab`/`contactGroup` correctly, including EAO's group where it previously returned `null`; unknown and prototype-named roles resolve empty; the registry-derived `leadTabConfigs()` reproduces the old literal tab list name-for-name and color-for-color; the backfill throws before `setupSpreadsheet`, inserts 3 of 4 matched rows (skipping a blank `Lead ID`), maps columns correctly onto a **deliberately reversed** destination header row, inserts 0 on a second run, and picks up exactly 1 newly-added row on a third; the EAO normalizer still dispatches through the registry and leaves `role` + `current_situation` intact for `bookingEventInternalDescription`. (2) A **differential** harness loads the pre- and post-refactor `Code.gs` side by side and compares every resolution across 13 roles and 12 categories plus both tab lists: **34 identical, 0 regressions**, 2 intended fixes (EAO contact group, EAO in the clear list), 6 prototype-key hardenings.

## 2026-07-08 (schema hardening + per-tab-vs-unified architecture decision)

- **fix(gas): killed the positional-coupling bug the `Heard About` column exposed, and documented the schema decision behind it.** `REPORTS_ENABLED_COL = LEAD_HEADERS.length` inferred a column's position from an array's length, so appending a 31st header silently slid it from index 30→31 while the live **Referral Partners** tab still had `Reports Enabled` at column 31 — `appendRow` would have written the `heardAbout` string into the `Reports Enabled` cell and the `TRUE` seed into unheadered column 32. Replaced with name-based lookup (`headerIndex(sheet, name)` / `reportsEnabledIndex(sheet)`) reading the sheet's real header row; `-1` is handled as a real state (seed write skipped + logged, opt-out read treats it as blank) rather than a guessed index. Any column can now be inserted anywhere in `LEAD_HEADERS` without reasoning about per-tab extras. Added idempotent `migrateAddHeardAboutColumn()` to backfill the nine existing lead tabs that `setupSpreadsheet`'s empty-tab guard skips, *inserting* before `Reports Enabled` (shifting header **and** data together) rather than appending after it. Recorded the **per-tab vs. unified Details-JSON schema** decision in `backend-architecture.md`: per-tab kept because the Contact Groups sync, cold-lead sweep, monthly referral summary, and `onEdit` row-moving all depend on tabs physically existing, and because the Sheet already holds production data; **manual-editing usability is explicitly not a factor either way**; unified JSON becomes the *natural* fit once `crm.` / `api.axispoint.llc` are real work, and should be revisited then, not before; the lightweight middle ground (one `Details JSON` overflow column, the pattern EAO's `Preferences` use already informally follows) is noted. **Requires `clasp push` + `clasp deploy -i`, then one manual run of `migrateAddHeardAboutColumn()`.**
- **verify(gas):** Node harness (`vm`-loaded `Code.gs`, fake `Sheet` with real `insertColumnBefore`/`getRange` semantics, stubbed GAS globals) — 31 assertions, all passing: `reportsEnabledIndex` resolves correctly with `Reports Enabled` last, first, mid-array, whitespace-padded, and absent (`-1`); migration on legacy 30-column tabs lands `Heard About` at column 31 on all nine tabs, shifts `Reports Enabled` to 32 **with its `TRUE`/`FALSE` data intact**, leaves prior columns untouched; a second run mutates nothing and reports `SKIP` for all nine; already-canonical tabs are no-ops; and an explicit regression guard asserts the old `LEAD_HEADERS.length` constant points at the wrong cell on a legacy tab where the new lookup does not.

## 2026-07-08 (audit follow-up — heardAbout persistence, calendar-link root cause, em-dash cleanup)

- **fix(gas): `heardAbout` is now persisted.** **Schema change (all lead tabs)** — `'Heard About'` **appended** to `LEAD_HEADERS` (30→31 columns, `COLS.HEARD_ABOUT = 30`), written by the new `leadHeardAbout(payload)` helper in `buildLeadRow`, and surfaced as its own `Heard about us` row in the internal `partner-notification` email (new `{{heardAboutRow}}` placeholder, directly under `Source`; embedded constant **and** `emails/partner-notification.html` mirror both updated, parity re-verified). `Source` (technical origin) and `Heard About` (self-reported attribution) remain two distinct fields and must never be merged. Deliberately absent from every client-facing surface: visitor confirmation, calendar event, and `.ics`. **Requires a one-time manual edit to the live Sheet header rows on every lead tab**, plus `clasp push` + `clasp deploy -i`. ⚠️ Non-obvious consequence: `REPORTS_ENABLED_COL = LEAD_HEADERS.length`, so on the **Referral Partners** tab `Heard About` must be *inserted before* `Reports Enabled` (which moves col 31→32), not appended after it.
- **fix(gas): found the real "View in calendar" bug — a third code path neither `#19` nor `#20` accounted for.** Clean-room re-trace of the current source (trusting neither prior PR) found `createBookingEvent`'s `CalendarApp` fallback set `created = true`, **cleared `error` to `''`**, and never set `calendarLink`. `#20`'s banner fires on `bookingRequested && !calendarCreated`, so it stayed silent: an event genuinely existed, no link rendered, and **no warning appeared** — byte-for-byte indistinguishable from a healthy booking, reachable even with `BOOKING_CALENDAR_ID` set and the deployment current. `#20` only distinguished *created* from *not created*. `createBookingEvent` now returns a third flag, `degraded` (event exists, no link capturable), preserving the advanced-insert error that forced the fallback; `handleFormSubmission` threads a `calendarStatus` object; `sendPartnerNotification` renders an amber **"⚠ Calendar event created, but no link captured"** notice distinct from the red **"NOT created"** banner. Also fixed in the same path: a Meet booking through the fallback provisions no conference, so the partner email rendered `<a href="">Join Google Meet →</a>`, a live-looking button with an empty `href` — now a plain "No Google Meet link was created" marker. **Full resolution of the originally-observed symptom still requires one live booking** through the deployed `/exec`; the three states are now mutually exclusive and self-identifying, so that test also adjudicates `#19` vs `#20`.
- **fix(gas): em dashes cleared from every runtime string.** The audit's "4 in `buildVisitorPersonalNote` + 2 subject lines" undercounted. Actually fixed: 4 in `buildVisitorPersonalNote`, **7** subject lines (visitor no-booking, partner notification, daily digest, cold-move summary, welcome subscriber, publish notification, unsubscribe), the subscriber-email signature, the resubmission note written into the Sheet's Message column, the `'—'` empty-value placeholders in two internal emails (→ `n/a`), and one `Logger.log` string. Verified by stripping comments and grepping the remaining code: **zero em dashes outside comments.** Comments left alone (not copy).
- **verify(gas):** Node harness (`vm`-loaded `Code.gs`, stubbed GAS globals) drives the real functions: `heardAbout` writes to `row[COLS.HEARD_ABOUT]` and never into `Source` (checked for direct, QR, and EAO-blank cases); it appears in the partner email and in **none** of the visitor email, `.ics`, event title, or client description; all six `createBookingEvent` outcomes produce the correct `created`/`degraded`/`error`/`calendarLink` combination; the partner email renders exactly one of {link, amber notice, red banner} per outcome and contains no empty `href` anywhere; zero em dashes across every rendered subject and body; all 7 embedded ↔ mirror template pairs still byte-identical.

## 2026-07-08 (docs refresh + audit against real source)

- **docs(infra):** Full `/docs` refresh covering this session's booking architecture: `CONFIG.BOOKING_CALENDAR_ID`, the `doGet ?action=availability` endpoint, the client/internal content split (documented as the **privacy** decision it is — the visitor is a calendar attendee and must never see CRM fields), and the fail-visible booking-failure warning that replaced fail-silent logging. Added the calendar-permissions gotcha (`executeAs: USER_DEPLOYING` means the *deploying* account needs "Make changes to events" on the bookings calendar; ownership satisfies this implicitly, non-owner access does not) and the recurring `clasp` `invalid_grant`/`invalid_rapt` reauth pattern.
- **audit:** Documented that **`heardAbout` is still not persisted.** The frontend sends it unconditionally on all four `buildPayload` roles; `Code.gs` never reads it; there is no `Heard About` column in `LEAD_HEADERS`. The `source`/`heardAbout` *separation* shipped in `#19`; **storage did not.** Adding it is a live-Sheet schema migration.
- **audit — corrected four stale claims** that prior task summaries had left in `/docs`:
  1. `backend-architecture.md` claimed `computeSlotAvailability` was "unit-tested in Node." **No test suite is committed** (`scripts/gas/` holds only `Code.gs`, `appsscript.json`, `emails/`). The verification harnesses were throwaway. A `Code.gs` docstring pointing at "scripts/gas tests" is likewise aspirational.
  2. `frontend-payload-schemas.md` still described `source` as coming from the "how did you hear" answer — the exact bug `#19` fixed — and claimed `heardAbout` is sent "ONLY when `opts.source` is set." It is sent always.
  3. `frontend-payload-schemas.md` claimed `pressing_issue` is "NOT echoed to visitor," directly contradicting `email-templates.md` and `buildVisitorPersonalNote`, which quotes it back.
  4. `backend-architecture.md` claimed Script Property values "are never committed as literals." `setProperties()` commits all three literally; the indirection buys redeploy-survival and a single definition site, not secrecy.
- **audit — "View in calendar" link is NOT confirmed fixed.** `#19` and `#20` recorded **incompatible root causes** for the same symptom (stale `/exec` deployment vs. fail-silent event creation), and neither was verified against a real delivered email. Recorded in `email-templates.md` as **unknown**, with the test that would settle it. The `#20` warning banner is now the discriminator.
- **audit:** Re-verified embedded ↔ mirror template parity independently (eval each `TEMPLATE_*` array out of `Code.gs`, diff against its `.html`). All seven match byte-for-byte. The `#20` claim holds.
- **known deviation (not fixed here, docs-only PR):** user-facing email copy contains em dashes in `buildVisitorPersonalNote` (4 sites) and 2 subject lines, violating the sitewide no-em-dash standard in `CLAUDE.md`.

## 2026-07-08 (later — booking privacy + calendar reliability)

- **fix(gas): PRIVACY — internal CRM data was leaking into the visitor's own calendar.** The visitor is an attendee on the real booking event, so the event description (and the `.ics` they receive) landed in their personal calendar. Both surfaces were rendering the full CRM dump (`Lead ID: AXP-…`, raw `Source`, asset class, internal `(Category)` label). Split into two helpers: **`bookingEventClientDescription(payload)`** (warm, minimal, no CRM) now feeds the Calendar **event** and the **`.ics`**; **`bookingEventInternalDescription(payload, leadId)`** (the full dump, renamed from `bookingEventDescription`) is used **only** in the internal partner-notification email. `bookingEventTitle` is now client-safe too (`AxisPoint Partners intro call with <name>`, no category label). The leadId still appears in the `.ics` `UID` (opaque machine identifier, never displayed) by design.
- **fix(gas): calendar event silently not created / "View in calendar" link missing.** Root cause of both: `createBookingEvent`'s fail-safe design (`!calId` early-return, the `Calendar.Events.insert` catch, and the upstream `handleFormSubmission` catch) only wrote to `Logger.log`, so a real config/access failure produced no event **and** an empty `calendarLink` while submission still returned success — indistinguishable from a healthy booking. `createBookingEvent` now returns `{ meetLink, calendarLink, created, error }`; `sendPartnerNotification` renders a loud "⚠ Calendar event was NOT created" warning (with the error + a pointer to `BOOKING_CALENDAR_ID` / calendar edit access) whenever a booking was requested but no event was created. **This makes the failure visible; the actual trigger is a runtime config state — verify `BOOKING_CALENDAR_ID` is set (re-run `setProperties()` in the Apps Script editor) and that the deploying account has edit access.** Not a timezone bug: slots run 8:00 AM–4:30 PM CT, so a parse offset could only shift the event's time within the day, never remove it.
- **fix(gas):** Warmed the visitor confirmation openers (meet/phone/no-booking) from the flat "We received your message. See you at the time below." to solutions-oriented copy; per-role `{{personalNote}}` personalization unchanged.
- **verify(gas):** Node harness (`vm`-loaded `Code.gs`) confirms client vs internal descriptions are genuinely different content, no CRM markers in any visitor-visible event/`.ics` line, the "View in calendar" link renders when a link is present, and the failure warning renders when it is not. Ships live only after `clasp push` + `clasp deploy -i` **and** confirming `BOOKING_CALENDAR_ID` is actually set.

## 2026-07-08 (later — live-test fixes)

- **fix(gas):** **Schema change (all lead tabs) — removed the redundant `Date Submitted` column.** `LEAD_HEADERS` drops from 31→30 columns; `Timestamp` (ISO) is now the single "when submitted" source. `sendDailyDigest` (today filter) and `moveColdLeads` (age calc + summary) read `Timestamp`, formatting to CT where a date comparison is needed. `REPORTS_ENABLED_COL` shifts 31→30. All callers use the `COLS` map (no hardcoded positions). **Requires a one-time manual edit to the live Sheet header rows** (delete the `Date Submitted` column on every lead tab) in addition to `clasp push` + `clasp deploy -i`.
- **fix(gas + brand):** **CRM `Source` data-integrity fix.** The frontend previously wrote the visitor's "How did you hear about us?" answer (e.g. "LinkedIn") into `payload.source`, which the backend stamped into the Source column. Now `buildPayload` sends `source` as the real origin only (`'qr'` or `''`) and always sends the answer separately as `heardAbout`; the backend's new `leadSource(payload)` maps that to `QR` / blank (no `payload.page` fallback). Source now reflects only the true origin of a submission.
- **feat(gas):** **`.ics` calendar attachment on visitor booking confirmations.** `buildBookingIcs(payload, leadId, meetLink)` builds a fully-detailed `VEVENT` (America/Chicago `VTIMEZONE`, Meet-link/phone `LOCATION`, shared title/description helpers) attached to the meet + phone confirmation emails — a belt-and-suspenders backup to Google's native attendee invite. New shared helpers `bookingEventTitle` / `bookingEventDescription` keep the event and the `.ics` in sync.
- **verify(gas):** Node harness confirms all of the above (valid iCalendar for meet + phone, Source no longer leaking how-heard, partner email renders `calendarLink`). **Issue-1 finding:** the committed `sendPartnerNotification` already renders `calendarLink` correctly — the link was missing in live email because the pinned `/exec` deployment predated the #18 change, i.e. a `clasp deploy` gap, not a code bug. Ships live only after `clasp push` + `clasp deploy -i`.

## 2026-07-08

- **feat(gas):** Booking calendar events now carry real context and invite all three parties. `createBookingEvent(payload, leadId)` adds attendees (zach@, ethaniel@, and the visitor) with `sendUpdates:'all'`/`sendInvites:true` so Google actually emails invites and the event lands on the partners' personal calendars; sets a descriptive title (`AxisPoint Call: <name> (<category>)`) and a plain-text description (lead ID, email, phone, asset class, source, per-type free-text field, phone-callback number); routes the phone path through the advanced `Calendar.Events.insert` too so the event `htmlLink` can be captured; and returns `{ meetLink, calendarLink }`. `sendPartnerNotification` renders the `calendarLink` ("View in calendar") in the booking block. Ships live only after `clasp push` + `clasp deploy -i`.

## 2026-07-07

- **feat(gas):** **Booking calendar migration — behavior change for ALL bookings.** `createBookingEvent` now writes every event (Meet and phone) to a dedicated shared "AxisPoint Bookings" calendar (`CONFIG.BOOKING_CALENDAR_ID`, read from the new `BOOKING_CALENDAR_ID` Script Property) instead of the deploying account's personal `CalendarApp.getDefaultCalendar()` / the `'primary'` calendar. Requires running `setProperties()` with the real calendar ID, then `clasp push` + `clasp deploy -i`.
- **feat(gas):** New read-only availability endpoint — `doGet ?action=availability&date=<…>` runs `Calendar.Freebusy.query` against the same `BOOKING_CALENDAR_ID` and returns which fixed `BOOKING_SLOTS` are free. Frontend `BookingCalendar` now fetches this on day-select and disables booked slots; on any failure it falls back to showing all slots available. No OAuth scope change (existing `auth/calendar` already covers Freebusy). Replaces the old static `TAKEN` set in `packages/brand`.
- **infra:** Hosting automation scripts built and verified live (`scripts/hosting`), full DNS/subdomain inventory captured, production migration plan documented (not yet executed). See deployment.md.

## 2026-07-06

- **docs:** Established `/docs` as the verified source of truth (backend architecture, email templates, frontend payload schemas, deployment) and corrected the stale root `README.md` against current source.
- **audit:** Confirmed pre-EAO dead code (`curious` / `Explorers` / `'refer'` / `Referrals Made`) is fully removed from `Code.gs`.
- **audit:** Found template drift — all 7 `scripts/gas/emails/*.html` mirrors carry a footer street-address line the embedded `TEMPLATE_*` constants lack, so it does not ship in live email (see email-templates.md).
- **fix(gas):** Added per-role `{{personalNote}}` to visitor confirmation emails — all 5 lead types now reflect back what they actually submitted (investor capital/experience, pro role/markets, referral profession/intent, EAO `pressing_issue`→`current_situation`, submit_referral referred-person name). New helpers `buildVisitorPersonalNote`/`humanList`/`referralIntentClause`; works across meet/phone/no-booking.
- **fix(gas):** Resolved the email-template drift — the footer address line is now embedded in all 7 `TEMPLATE_*` constants; embedded ↔ mirror verified byte-for-byte in sync.

## 2026-07-05 — `fix(gas)` (#10)

- Wired the `existing_asset_owner` (EAO) lead type end-to-end: `normalizeEaoPayload` reshapes the flat EAO payload into the generic lead shape; added `Existing Asset Owners` tab + `Existing Asset Owner` category mapping.
- Fixed `submit_referral` routing: it correctly resolves to the `Referral` category with **no** per-role tab (`categoryTabForRole` returns `null` by design), logging the relationship to the Referrals tab.
- Dead-code removal of the old role vocabulary.
- Config groundwork: `COLD_LEAD_DAYS` centralized in `CONFIG`; `Reports Enabled` opt-out column on the Referral Partners tab (`REPORTS_ENABLED_COL`) driving `sendMonthlyReferralSummaries`.
- OAuth scopes pinned in `appsscript.json` (calendar, gmail.send, spreadsheets, contacts, script.scriptapp) + advanced Calendar v3 service.

## Notes

- Backend deploys are two-step: `clasp push` updates HEAD only; the live `/exec` URL requires `clasp deploy -i <deploymentId>` (see deployment.md).
