# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.vite/
.turbo/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Temporary files
*.tmp
.cache/
.temp/

# OS files
Thumbs.db
.env.local

# clasp (contains the Apps Script ID)
# These are literal paths, not a pattern — a new GAS project needs its own line here
# BEFORE the directory is created, or its script ID gets committed on the first add.
#
# The first line outlives the V1 source deleted on 2026-08-15, on purpose: developer
# machines that worked on V1 still hold that file, it still carries the live Script ID
# of the external Apps Script project, and dropping the line would surface it as an
# untracked file waiting to be committed by accident.
scripts/gas/.clasp.json
scripts/gas-v2/.clasp.json

# hosting-automation credentials (cPanel + Namecheap) — never commit
scripts/hosting/.env

# Claude Code machine-local state. Holds settings.local.json and, more importantly,
# `worktrees/`, where Claude Code checks out full copies of the repository at other
# commits. Those copies are real working trees, sometimes at pre-retirement commits,
# and they are not repository content. Ignoring the directory keeps a broad `git add`
# or a recursive grep for retired code from picking them up.
.claude/
